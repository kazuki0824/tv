# Maleicacid Tuner HAL legacy source

このディレクトリは旧 `tuner_hal` の参照用ソースである。
product default の Tuner HAL service は `tuner_hal2` とし、既定 product 統合手順は `tuner_hal2/INTEGRATION.md` を正とする。
旧 `tuner_hal` を同一 product で `ITuner/default` として有効化してはならない。

## 参照文書

- 設計方針: `DESIGN_JA.md`
- 実装規約: `CODE_CONVENTION.md`
- legacy 統合参考: `INTEGRATION.md`
- 変更履歴: `CHANGELOG.md`
