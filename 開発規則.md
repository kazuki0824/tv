## リリース段階ごとの到達点

本プロジェクトでは、AOSP標準の責務分担に従い、Tuner HAL、TIS、arib_si_engine_rs、CAS HAL、rec、epgcollection の責務を分離する。

最終製品ゴールは、AOSP標準の責務分担および契約（構成・インターフェース・実装などの仕様）に完全に準拠し、対象範囲の機能がすべて実装済みであることである。ただし、各リリースの完了判定では、そのリリースで **表明または有効化した機能範囲** について、AOSP の API 契約、責務分担、エラー契約、状態遷移、実動作が一致していることを完了条件とする。未実装機能を成功扱いにしてはならず、未対応機能は 能力情報に出さない、または 公開APIで `UNAVAILABLE` / `INVALID_ARGUMENT` / `INVALID_STATE` 等として明示的に拒否する。


AOSP標準の `packages/apps/TV` の Live TV / Reference TV app から、TvInputManager / TvInputService / Tuner SDK API 経由で視聴できることを確認対象とする。TIS は Tuner HAL binder を直接叩いてはならない。

ARIB放送規格および日本での放送運用に準拠することを、AOSP標準準拠とは別の完了条件として扱う。

ARIB適合性では、製品が準拠すべき規範対象と、レビューで適合性を確認するための検証証拠を分離する。規範対象は、製品scopeに適用される現行ARIB日本語原文とし、レビュー環境の会員資格、購入可否、ネットワークアクセスによって変更しない。ARIB公式英語版は原文の翻訳として扱い、原文との内容・表現等に差異がある場合は原文を優先する。

検証証拠は、対象条項の現行日本語原文を取得できる場合はその原文を用いる。会員認証または有償入手等により現行日本語原文をレビュー環境から取得できない場合は、ARIB公式英語版本文を条項確認の証拠として使用してよく、取得可能な日本語旧版より新しい公式英語版がある場合もその英語版を使用してよい。ただし、使用する英語版が現行日本語原文と同版でない場合は、未取得の現行原文との差分を未証明として明記し、その英語版だけを根拠に現行最新版への完全適合を確認済みとは扱わない。版一覧、改定概要、規格概要は版差分と適用範囲の確認には用いてよいが、未取得本文の具体的な条項内容を推測する根拠にはしない。

製品ライブAVのアーキテクチャは、AOSP Tuner framework の non-passthrough clear-memory ES path に固定する。TIS は `MediaEvent.getLinearBlock()` から MediaCodec の linear-block input へ渡す経路を使用し、secure-memory handle、tunneled playback、platform passthrough を製品のライブ再生能力として広告・使用しない。この選択はリリース時点に依存する暫定scopeではなく、製品アーキテクチャの恒久不変条件である。r52 の B25 / B1 対応も、Tuner descrambler 後の clear ES をこの経路へ接続できるサービスをライブ視聴成功の対象とし、secure media path を必須とするサービスを成功対応として表明しない。

製品レベルの安定性として、zapping、低品質信号の受信、単一TS複数サービス、解像度可変、スクランブル有無が小刻みに切り替わる stream、長時間動作、close/reopen、stop/start、再選局を確認対象に含める。

### モジュールと言語境界

- `tis`:
  - TvInputService、setup、channel 登録、ライブセッション、TvProvider 連携を担当する。
  - TIS の TS packet / section の低レベル解析以外を Kotlin のみで実装する。すなわちTvInputService、setup、channel登録、ライブセッション、TvProvider連携、Android API接着層は Kotlin で実装する。
  - TS/SI/EIT などの構造化解析は arib_si_engine_rs に置く。
  - Tuner HAL には Tuner SDK API 経由でアクセスする。
  - 例外として、ARIB字幕 path の libaribcaption C API 接続は Rust JNI boundary + 安全なRustラッパー を許可する。

- `arib_si_engine_rs`:
  - Tuner HAL から Tuner SDK API / TIS 経由で渡された PSI/SI/EIT section payload を入力として、PAT/PMT/CAT/SDT/NIT/BAT/EIT/descriptor の 意味解析 を行う。
  - raw TS packet processing、PID filter、section assembly、section delivery は Tuner HAL の責務であり、arib_si_engine_rs には実装しない。
  - 字幕処理と libaribcaption ラッパー を持ち込んではならない。
  - 字幕本文処理は TIS 側の字幕 path に閉じる。

- `tuner_hal`:
  - `tuner_hal/DESIGN_JA.md` を Tuner HAL の公開契約・capability・状態遷移・資源寿命の設計正本とし、旧 Tuner HAL 実装ソースは AOSP / ARIB / driver 固有ロジック、差分確認、単体部品の参照用として repository に残す。
  - `px4_drv` と `earth_pt1` は tune モデル が異なるため、選局候補表の SSOT は本書に置く。製品 scan 候補表の保持と実行時候補生成は TIS が担当し、Tuner HAL 内部では canonical frequency モデル と backend 変換を分離する。
  - r50ee5以降、product default の Tuner HAL service には含めない。product package、VINTF manifest、init rc、PRODUCT_PACKAGES、product integration へ旧 `tuner_hal` service を入れてはならない。
  - 設計正本、旧実装の参照、差分確認、単体部品参照には使ってよいが、`android.hardware.tv.tuner.ITuner/default` を登録する実体として扱わない。

- `tuner_hal2`:
  - r50ee5以降の product default Tuner HAL service とし、`earth_pt1`（Linux Kernel builtin）と `px4_drv`（kazuki0824/px4_drv にある DDK 準拠チューナードライバ）向けの Tuner HAL 実装責務を一意に持つ。
  - FMQ / dma_heap との界面を除き Rust で実装する。
  - r51 で対応宣言する `ITuner` / `IFrontend` / `IDemux` / `IFilter` / `IDvr` / `IDescrambler` / `ILnb` 面は、成功 no-op を残さず `tuner_hal/DESIGN_JA.md` のAOSP公開契約に従って実装する。ただしLNBについては、本書「LNB部分能力公開の製品例外」に定義するproduct-level例外を適用する。
  - `tuner_hal` 全体を丸ごとコピーしてはならない。AOSP / ARIB / driver 固有の実ロジック断片だけを選別して取り込む。
  - `android.hardware.tv.tuner.ITuner/default` を登録する実体は `tuner_hal2` だけとする。
  - 旧 `binder_service/src/tuner_hal.rs` の巨大制御層、空構造体、薄い取引型、文字列分類、失敗破棄経路を持ち込んではならない。

### 設計文書の責務分担

設計情報は、次表の所有文書だけを規範正本とする。別文書に同じ事実が必要な場合は正本を参照し、規範値・状態遷移・製品判断を独立に再定義しない。既存の越境記述を是正する場合は、情報を削除するのではなく所有文書へ移し、移動元を参照へ置換する。移動前に存在した規範情報の意味集合が移動後にも全て残ることを受け入れ条件とする。

| 文書 | 規範として所有する内容 | 所有しない内容 |
|---|---|---|
| `開発規則.md` | リリース到達点、製品全体または複数moduleにまたがるscope、製品scan候補の規範値、module間責務、product-level invariant | Tuner HAL個別APIの戻り値・状態遷移・VTS profile詳細、TIS内部runtime状態、PSI/SI parser内部意味モデル |
| `tuner_hal/DESIGN_JA.md` | AOSP Tuner HAL公開契約、capability/VTS境界、公開状態・戻り値・寿命・確定点・rollback/cleanup、TS/section/PES/AV伝送契約 | `tuner_hal2`の現在実装位置・移行進捗、TISのchannel登録/TvProvider判断、PSI/SI意味解析 |
| `tuner_hal2/DESIGN_JA.md` | `tuner_hal/DESIGN_JA.md`を実装へ接続する論理責務、依存方向、実装owner/anchor | 公開AIDLの規範値・状態遷移・戻り値・確定点・rollback/cleanupの独自定義 |
| `tuner_hal2/INTEGRATION.md` | product package、VINTF/init/ueventd/sepolicy、静的VTS設定の配置などproduct統合 | filter/section/PES/DVR等のruntime意味論、TIS/SI engineの実行時判断 |
| `tis/DESIGN_JA.md` | TvInputService runtime、scan実行、channel登録、TvProvider利用、ライブ視聴、Android APIへの写像 | HAL binder契約、driver固有ABI、PSI/SI構文・descriptor意味解析そのもの |
| `arib_si_engine_rs/DESIGN_JA.md` | PSI/SI/EIT/descriptorの構文・意味解析、意味object、parser診断、provider-data schema/canonical encode | Android channel登録可否、TvProvider列への最終投影判断、製品リリースの収集scope、HAL状態遷移 |
| `ARIB_SI_EPG_TvProvider投影方針.md` | ARIB意味objectからTvProvider標準列/internal_provider_dataへの投影判断 | parser構文、TIS runtime、HAL公開契約 |
| `GLOBAL_CODE_CONVENTION.md` | 全moduleに共通する実装規約、禁止構文、一般worker / lock / FFI / parser / test規約 | module固有のowner / helper / entry、公開状態遷移、戻り値、capability、資源寿命 |
| `tuner_hal2/CODE_CONVENTION.md` | product default Tuner HALの実装規約、禁止構造、typed entry / helper使用規則、静的検査観点 | 公開状態遷移、戻り値、capability、資源寿命、transaction意味論の独自定義 |
| `tuner_hal/CODE_CONVENTION.md` | 旧`tuner_hal`参照実装にだけ適用する具体type / helper / module規約 | product default `tuner_hal2`の現行実装規約、project-wide規約、公開契約 |

`tuner_hal/DESIGN_JA.md` が正本として持つ公開transactionのphase order、確定点、rollback / cleanupと、`tuner_hal2/DESIGN_JA.md` が正本として持つ実装owner、実装anchor、禁止入口は一体の設計契約として扱う。これらの所有者、順序、確定点、巻き戻し、禁止入口のいずれかを変更する場合は設計変更として扱う。

#### LNB部分能力公開の製品例外

本製品は `earth_pt1` / `px4_drv` のLNBについて、Android 14 CTSのLNB試験合格よりも、hardware / driverで実処理可能と確認できたLNB operation / valueを公開することを優先する。このproduct-level判断は、上記の「対応宣言する公開面をAOSP契約どおりに実装する」という一般則に対するLNB限定の明示的例外である。

この例外を適用した部分LNB endpointはAndroid 14 CTSのLNB試験に加え、Android 14 AIDL Tuner VTSのLNB live試験でも失敗し得るため、本製品は当該LNB面についてCTS/VTS合格およびCDD適合を宣言しない。未対応operationを成功no-op、擬似成功、callback echoで実装済みに見せることも、公開済みLNBをVTS XML/profileから到達不能にして検査を回避することも禁止する。

この節が所有するのは「部分LNB能力を公開し、CTS/VTS/CDD compatibility deltaを既知として受容する」という製品判断だけである。公開endpointの選定条件、operation / valueごとの成功・`UNAVAILABLE`・`INVALID_ARGUMENT`、資源寿命、callback、rollback / cleanup等のTuner HAL公開契約は`tuner_hal/DESIGN_JA.md`を正とする。理由、CTS/VTS側の具体的試験内容、既知差分、再評価条件の記録は`future_work/not_planned/tuner_lnb_partial_hardware_cts_mismatch.md`を参照する。
#### STD-B25 Part 1 §4.9 最小鍵組容量の製品適合方針

STD-B25 Part 1 §4.9 が受信機システムに要求する最小8鍵組容量は、本製品全体として恒久的に適合対象外とし、同条項への適合を宣言しない。`StdB25DecodeCapability` に記録する限定したTS payload decode能力、実鍵組数、実PID数を、同条項への適合、Part 1 CAS-R全体への適合、またはSTD-B25全面準拠の宣言へ読み替えてはならない。

この節が所有するのは上記product-level判断だけである。`StdB25DecodeCapability`、`DescramblerCapacityPool`、個別APIの受付・解放・失敗時契約は `tuner_hal/DESIGN_JA.md` を正とする。

#### 製品 scan 候補の規範値

CATV候補表は C13〜C63 に固定する。MID band は C13〜C22、SHB band は C23〜C63 とし、中心周波数は ARIB STD-B21 5.12-E2 Appendix 10 Table 10-3・Table 10-4 の `+1/7 MHz` オフセット込みで保持する。C22 は `167 + 1/7 MHz`、C23 は `225 + 1/7 MHz` であり、C21からC22、C22からC23は単純な6MHz連続として計算しない。

VHF 1〜12ch は開発規則.mdで恒久的にスコープ外であり、TISのCATV候補表、地上波候補表、共同受信候補表に追加してはならない。

BSの通常実行時候補は、TISが保持するBS TSID表からIF周波数と`STREAM_ID 0..65534`として生成する。TISはHALのeffective capabilityやdriver名で候補を分岐しない。Tuner HALはselector kindを保持して各backend ABIへ写像する。px4の相対slot表またはlegacy数値域をTISへ複製してはならない。

### r51 の到達点

r51 は、非スクランブル 平文 放送を対象に、AOSP標準経路で Live TV 視聴と Tuner HAL 基本機能を成立させるリリースである。

r51 で必須とする範囲は次のとおり。

- Tuner HAL:
  - `ITuner` / `IFrontend` / `IDemux` / `IFilter` / `IDvr` / `IDescrambler` / `ILnb` のうち、r51 で advertise / 対応宣言する機能範囲を AOSP 契約どおりに実装する。ただしLNBについては、本書「LNB部分能力公開の製品例外」に定義するproduct-level例外を適用する。
  - ISDB-T、BS、CS110 の explicit tune を対象にする。
  - 製品Japanese scan planはTISが所有してexplicit tune candidateを生成し、Tuner HALはproduct-specific Japanese scan planを生成しない。これはreleaseに依存しないmodule責務の恒久不変条件とする。
  - 平文ライブ視聴 AV path、section / PES / record / DVR path、DVR playback path を r51 対象にする。
  - `IDescrambler` API 境界、`setKeyToken()`、`addPid()` / `removePid()`、トークン lookup 境界、未接続・bad トークン・expired トークン 診断を実装する。
  - CAS HAL 仮実装 のまま 本番経路のスクランブル解除成功 を 対応宣言しない。
  - TS-only を r51 の対象とし、MMT / TLV / ISDB-S3 / B61 / 4K 放送は恒久的に対象外とする。

- TIS:
  - `TvInputService` として システムTVアプリ から呼ばれる。
  - Tuner HAL には HAL binder 直叩きではなく Tuner SDK API 経由でアクセスする。
  - setup / scan / channel 登録 / ライブセッション / 平文視聴 / 最低限の Programs 登録を実装する。
  - `Programs.COLUMN_CANONICAL_GENRE` は、ARIB content descriptor から Android canonical genre へ明示写像できる分類だけを直接設定する。写像不能分類は `COLUMN_BROADCAST_GENRE` と provider-data JSON のみに保持する。
  - `series_descriptor` の `series_id` は単一値なら `Programs.COLUMN_SERIES_ID`、複数値なら `Programs.COLUMN_MULTI_SERIES_ID`、episode number は `Programs.COLUMN_EPISODE_DISPLAY_NUMBER` へ投影する。last episode number は通常の `TvContract.Programs` に自然対応する標準列がないため標準列へ投影せず、repeat label、program pattern、expire date、series name と合わせて provider-data JSON の series 構造に保持する。
  - `event_group_descriptor` は r51 で provider-data JSON の `relatedItems` へ `shared` / `relay` / `movement` として構造化保存し、予約追従利用は r53 まで行わない。
  - EIT `free_CA_mode`、音声 ISO639 language、視聴年齢制限 を TvProvider メタデータ へ自然対応として投影する。取得不能値や未対応値を推測で設定してはならない。
  - 字幕 track 検出、`TvTrackInfo` 通知、`onSetCaptionEnabled()`、ARIB 字幕 PES の libaribcaption C API 経路、字幕表示までを r51 の対象に含める。BML / data broadcast 実行環境、双方向データ放送 UI、データ放送 UI は恒久的に対象外とする。
  - CAS HAL が本実装されている前提を置かない。
  - 仮トークン または診断専用 トークン を 本番経路のスクランブル解除成功 として扱わない。
  - トークン 用語は次のとおり固定する。`production token` は r52 以降に CAS HAL 本体が発行する復号用の不透明参照値である。`fake token` は fake CAS / test 用、`diagnostic token` は診断注入用、`placeholder token` は 仮実装 CAS 境界確認用であり、いずれも 本番経路 使用不可である。

- arib_si_engine_rs:
  - TIS が必要とする SI / EIT / サービス検出 / Programs 登録用の構造化解析を担当する。
  - r51 の `TvProvider.Programs` 収集は EIT p/f actual `0x4E` に固定する。EIT p/f other `0x4F`、EIT schedule actual `0x50..0x5F`、EIT schedule other `0x60..0x6F`、長期 schedule window、サービス横断更新は r53 対象とし、r51 の scan/setup で schedule を短期補完として収集しない。
  - r51 では content、component、音声コンポーネント、視聴年齢制限、series、イベントグループ、linkage、unknown descriptor を構造化変換し、TvProvider 標準列へ自然対応する値と provider-data JSON へ保存する値を分離する。
  - 常時 EPG 収集、長期 schedule 収集、予約録画・追従録画向けの高度利用は r53 対象とする。

- CAS HAL:
  - r51 では 仮実装 とする。
  - 対応 CAS system id を 本番経路 capability として広告しない。
  - plugin を 本番経路のスクランブル解除成功 可能なものとして返さない。
  - TIS の ECM 送受信境界、B25 用 EMM 送受信境界、Tuner HAL の `IDescrambler` 境界、不透明 トークン 受け渡し試行までを接続確認対象とする。B1 用 EMM 境界は設けない。
  - スクランブル解除成功は r51 の 対応宣言 外とする。

r51 で 表明してよい視聴範囲は、対象周波数帯における **非スクランブルサービス** である。スクランブル サービスについては、サービス検出、CA情報、ECM セクションフィルター起動境界、B25 用 EMM セクションフィルター起動境界、MediaCas / CAS bridge 接続境界、Tuner descrambler 接続境界、診断までを対象とし、視聴成功は表明しない。B1 用 EMM 境界は設けない。

選局については、r51 で対象周波数帯に対する explicit tune と サービス検出 を成立させる。r51 では対象周波数帯のうち、publishable ∧ viewable ∧ r51対応codec を満たす非スクランブルサービスを Live TV 視聴の 対応宣言対象とする。具体的には、ARIB `service_type=0x01` は現行対応video ESを持つaudio-video / video-only、`service_type=0x02` は現行対応audio ESを持つaudio-onlyを含み、最終のchannel登録・再生条件は `tis/DESIGN_JA.md` を正とする。スクランブル サービスの視聴成功は r52 対象とする。



### r51 視聴年齢制限 / CAS未完成EPG追加契約

r51 の TIS は 視聴年齢制限 を正式 対応宣言対象に含める。TIS は `parental_rating_descriptor` の JPN / レーティング 4..20 を Android `TvContentRating` の AOSP system-defined ISDB レーティング（`domain=com.android.tv`, `ratingSystem=ISDB`, `rating=ISDB_<age>`）へ変換し、`Programs.COLUMN_CONTENT_RATING` へ `flattenToString()` 形式で保存する。Android `TvContentRating` domain / ratingSystem / レーティング 文字列は TIS 側の責務であり、`arib_si_engine_rs` のSSOTにしてはならない。

`Programs.COLUMN_CONTENT_RATING` は表示・検索用投影であり、視聴制限 enforcement の完了条件ではない。Live session は 現在番組 レーティングを `TvProvider current Program -> latest EIT cache -> TvContentRating.UNRATED` の順で解決し、`TvInputManager.isRatingBlocked()` を使って判定する。blocked の場合は `notifyContentBlocked(rating)` を呼び、AV再生を開始または継続しない。parental block の通知手段として `notifyVideoUnavailable()` を使ってはならない。

CAS 未完成 scrambled サービス でも channel / Programs / EPG / コンテンツレーティングは登録してよい。ただし 視聴制限 allowed であっても 平文ライブ視聴 success として扱ってはならない。CAS 未完成 / scrambled unsupported により playback success にしない場合、TIS は `TvInputManager.VIDEO_UNAVAILABLE_REASON_CAS_UNKNOWN` を使い、CAS HAL 本実装なしに `CAS_NO_CARD` 等の具体状態 reason を使ってはならない。

### r52 の到達点

r52 は、CAS HAL 本体を実装し、B25 / B1 の 本番経路のスクランブル解除成功 を 対応宣言 可能にし、現行 product が対象とする transport profile であり得る codec を ARIB 資料ベースで認識・再生判定できるリリースである。

ここでいう codec 認識は、ARIB 資料上あり得る codecメタデータを TvProvider / trackメタデータ / 診断情報へ保存できることを意味する。codec を メタデータ として認識することは、当該 codec または当該 transport profile の ライブ 視聴可能性 / 再生可能性 を 対応宣言することを意味しない。ISDB-S3 / MMT / TLV / 4K 由来の codec は、メタデータ / 診断情報へ保存しても ライブ 視聴可能性 / 再生可能性 には含めない。

r52 で必須とする範囲は次のとおり。

- CAS HAL:
  - B25 / B1 用 CAS plugin descriptor を広告する。
  - `IMediaCasService` / `ICas` session lifecycle を実装する。
  - B25 は ECM / EMM section payload を処理する。B1 は ECM section payload のみを処理し、EMM を対応能力としない。
  - B25 の scheme / vendor 固有 ECM / EMM 処理、B-CAS / card I/O、CAS方式固有の entitlement validation、EMM / card 処理による Kw 管理、ECM からの Ks 導出は CAS HAL / MediaCas plugin / vendor key bridge 側に閉じる。B1 は scheme / vendor CAS 側の ECM-only 経路だけを正式対応とし、EMM 処理、EMM に依存する activation / control information の取得、契約・権利更新を対応しない。
  - Ks は Tuner descrambler が opaque token で参照可能な安全な内部鍵資源へ設定し、その資源と session の対応を表す opaque token を vendor key bridge 経由で成立させる。物理 key slot の実装方式は vendor 内部契約に閉じる。
  - raw Kw / Ks を Binder または TIS に出さない。
  - 本番経路 トークン を Tuner descrambler が解決できる opaque トークン として発行する。
  - libyakisoba ベースの参照実装を作成し、r52 の 本番経路 CAS HAL 実装方針と照合する。

- TIS:
  - MediaCas / CAS bridge を型付き API で呼び、取得した 本番経路 トークン だけを Tuner descrambler へ渡す。
  - B25 では ECM / EMM、PMT 変化、session/トークン lifecycle に追従する。B1 では ECM、PMT 変化、session/トークン lifecycle に追従し、EMM filter を起動せず `MediaCas.processEmm()` を呼ばない。
  - スクランブル サービスの ライブ視聴を r52 の 対応宣言対象にする。ただし、製品アーキテクチャの恒久不変条件である clear-memory non-passthrough path へ復号後ESを接続できるサービスに限り、secure media path を必須とするサービスを成功対応に含めない。
  - 現行 product が対象とする transport profile の video/audio codec は、本書で規範対象とした現行ARIB日本語原文に適合させる。設計レビューでは `tis/DESIGN_JA.md` に、実際に検証証拠として使用したARIB公式本文の版・分冊・条項と、現行原文との差分が未証明である範囲を固定し、MediaFormat / decoder 起動 / AudioTrack / first-frame gate / unsupported 診断情報に接続する。版一覧や改定概要だけから未取得本文の具体的な codec 契約を推測して追加しない。
  - r52 では、現行対象の従来TS profileでARIB signaling上HEVC/H.265が現れる場合をライブ再生判定対象に含める。PMT / descriptor のHEVC認識、MediaFormat、MediaCodec capability照合、decoder起動、MediaSync first-output gate、unsupported診断までを同一契約で扱い、r51のMPEG-2/H.264限定をr52へ持ち越さない。
  - 対象 transport profile が恒久対象外の codec は メタデータ と 診断情報で認識しても、ライブ 視聴可能性 / 再生可能性 として 対応宣言しない。
  - 対応 video がある場合、未対応 audio だけを理由に `notifyVideoUnavailable()` にせず、video-only 診断へ落とす。

- Tuner HAL:
  - r51 で実装した `IDescrambler` 境界を 本番経路 CAS トークン と接続する。
  - descramble success / failure / scrambled pass-through を Live/AV path と record/raw TS path で混同しない。
  - 本番経路のスクランブル解除成功 を Tuner HAL / CAS HAL / TIS の統合確認対象にする。

### r53 の到達点

r53 は、予約録画、常時 EPG 収集、EIT 追従、長期 schedule 利用を製品機能として 対応宣言 可能にするリリースである。

r53 で必須とする範囲は次のとおり。

- rec:
  - 予約録画、時刻指定録画、EIT p/f 追従、番組延長・変更・中止への追従、競合判定を実装する。
  - `TvInputService.RecordingSession`、DVR/file output、`RecordedPrograms` 登録、`notifyRecordingStopped()` / `notifyError()` を AOSP/TIF 契約どおりに実装する。

- epgcollection:
  - 常時 EPG 収集、長期 schedule 収集、サービス横断の EPG 更新を実装する。
  - 実装後は、選局対象のうち `EPGがあるサービス` だけにフィルタをかける。
  - TIS / arib_si_engine_rs / TvProvider の責務境界を保ち、Tuner HAL を EPG 意味モデル の SSOT にしない。

- arib_si_engine_rs:
  - r53 の EPG 収集・予約・追従に必要な EIT schedule / descriptor / event identity / 差分更新を提供する。
  - 字幕本文処理は TIS 側の libaribcaption 経路に閉じる。BML / data broadcast 実行環境は引き続き恒久対象外とする。
  - イベントグループ、linkage、EIT schedule other、長期 schedule window、予約追従用 event identity を扱い、r51 で保存した `relatedItems` / linkage を安全条件付きで予約追従へ接続する。

### 恒久的なスコープ外

- 4K放送、ISDB-S3、STD-B61、MMT、TLV は恒久的にスコープ外とする。
- STD-B79 の ISDB-T2 / ISDB-T1.5 および STD-B80 の ISDB-T3 は製品が採用しない高度地上方式であり、frontend capability、scan、ライブ視聴、EPG収集、codec capabilityを含め恒久的にスコープ外とする。
- BML / data broadcast 実行環境、双方向データ放送 UI、データ放送 UI は恒久的にスコープ外とする。
- VHF 1〜12ch、共同受信固有の特殊候補表、CATV の VHF 1〜12ch 相当候補は恒久的にスコープ外とする。
- CATV候補表は C13〜C63 のみを対象とし、VHF 1〜12ch を CATV 候補として追加してはならない。

この節は、製品全体または複数モジュールに影響する恒久的なスコープ外だけを正本として持つ。Tuner HAL の API、capability、VTS/profile、AIDL戻り値に閉じる恒久非対応は `tuner_hal/DESIGN_JA.md` を正とし、本書へ重複宣言しない。

# 設計方針

## 組み込まれるターゲット
使用するAndroidは、[get_android_qcow2.sh](https://github.com/kazuki0824/android_device_maleicacid_androidtv_tools/blob/main/get_android_qcow2.sh) でビルドされるものを前提とします。

## Tuner HALが考慮すべきドライバの仕様
Tuner HALは、下記の事実を踏まえて設計を行うこと。
- px4 legacy driverの`slot` ABIは、`slot < 12`を相対番号、`slot >= 12`をabsolute TSIDとして解釈する。HALはAOSPのtyped `RELATIVE_STREAM_NUMBER`と`STREAM_ID`を区別し、表現可能な値をlegacy `slot`へ直接渡す。数値域からselector kindを推測せず、driver固有値をTISへ公開しない
- 今回のターゲットデバイスドライバのうちearth_pt1において、 DVB frontend が複数 delivery system を持つ場合がありえない
- 今回のターゲットデバイスドライバに AOSP Tuner HAL の resource として扱うべき物理 demux は存在しない

## BS/CS選局方法
日本のISDB-Sでは、BSは同一周波数帯で複数TSを扱い、CS110は本製品の選局契約上、周波数だけで選局する。

TISが生成するBSの通常製品requestは、backendにかかわらずIF周波数とAOSP `STREAM_ID`のTSID `0..65534`とする。earth_pt1 / DVB backendはTSIDを`DTV_STREAM_ID`へ渡す。px4 backendはtyped selectorを保持し、`RELATIVE_STREAM_NUMBER 0..7`はlegacy `slot`へそのまま渡し、`STREAM_ID 12..65534`もlegacy `slot`へそのまま渡す。absolute `STREAM_ID 0..11`はAOSP上有効だがpx4 ABIでは相対番号と区別できないため、px4では副作用なしの`UNAVAILABLE`とする。`65535`は明示TSIDとして`INVALID_ARGUMENT`とする。

HALはselector kindを数値域から推測せず、TISへdriver種別、相対slot、HAL内部capabilityを公開しない。AOSP公開`RELATIVE_STREAM_NUMBER`はHALの正式入力として保持するが、TISの通常scan・channel保存・再選局では使用しない。

## 日本向け scan / 選局契約

選局対象、対象周波数帯、BS/CS110 selector 境界、CATV 候補範囲、VHF 1〜12ch の除外、CATV C13〜C63 限定、BS/CS110 の selector 方針に関する設計契約 SSOT は本書に置く。

製品用 channel scan の実行主体と候補表の実装データ保持者は TIS とする。TIS は本書で固定した設計契約に従い、地上UHF、CATV C13〜C63、BS、CS110 の候補表を実装データとして持つ。候補表の具体値、BS TSID 表、CATV中心周波数表、表示番号、サービス検出結果から作る channel key は TIS 側の実装データを正とする。

Tuner HAL は日本向け scan plan、サービス candidate table、channel display number、remote control key mapping、TvProvider 登録用 メタデータを自前生成しない。Tuner HAL common、frontend_dvb、generic scan layer には、日本向け scan table、サービス candidate table、BS/CS110 TSID table を置かない。

px4 backendはscan policyを持たず、AOSPのtyped selectorをpx4 legacy APIの`slot`へ写像するadapterに限定する。`RELATIVE_STREAM_NUMBER 0..7`と`STREAM_ID 12..65534`はkindを保持した検証後に値をそのまま渡す。`STREAM_ID 0..11`はlegacy ABI上の相対番号と衝突してabsolute値として表現できないため`UNAVAILABLE`とし、別TSへfallbackせずbackendを変更しない。固定TSID→slot表、TMCC由来の暗黙変換、数値域によるselector kind推測を導入しない。

他文書に scan 候補値を例示する場合は、必ず「説明用」「非SSOT」と明記し、本書の設計契約および TIS の実装データを参照する。`tuner_hal/DESIGN_JA.md`、`tis/DESIGN_JA.md`、README、future_work に独立した候補表SSOTを作ってはならない。

frontend_px4はscan policyを持たず、AOSPのIF周波数とtyped selectorをpx4_drvの`freq_no / slot / addfreq`へ変換するだけにする。相対番号とabsolute TSIDはselector kindで検証し、legacy ABIで表現可能な値だけを`slot`へ直接渡す。本書の選局契約とTISの製品用候補表を置き換える変換表を持ってはならない。

px4のISDB-S frontendを製品へ公開する条件は、対象driverのlegacy `slot` ABIと、`RELATIVE_STREAM_NUMBER 0..7`および`STREAM_ID 12..65534`の直接写像が検証済みであることとする。absolute `STREAM_ID 0..11`を成功広告せず、指定時は`UNAVAILABLE`として副作用を生じさせない。TIS側を相対番号へ分岐させて補償せず、frontend_px4へTSID→relative slot変換表を導入してはならない。

VTS / lab profile と backend adapter の内部変換は、製品 scan 候補表と サービス検出 SSOT ではない。これらを根拠に TIS 側の scan table、BS/CS110 TSID table、TvProvider channel key、display number を生成してはならない。

## BS/CS110 stream selector境界
BSの通常製品経路では、TISはtyped `STREAM_ID`のTSID `0..65534`を生成する。HALはselector kindを正として値域から種類を推測しない。earth_pt1 / DVBでは`STREAM_ID 0..65534`をabsolute値として処理する。px4では`RELATIVE_STREAM_NUMBER 0..7`と`STREAM_ID 12..65534`をlegacy `slot`へ直接渡し、absolute `STREAM_ID 0..11`は相対値域と衝突して表現不能なため`UNAVAILABLE`とする。衝突値を`INVALID_ARGUMENT`またはrelative selectorとして解釈してはならない。

TISは`RELATIVE_STREAM_NUMBER`を通常候補またはchannelデータへ使用しない。CS110では、channel key/サービス識別子にONID/TSID/service_idを保持してよいが、HAL frontend selectorへ転用しない。stream selectorはNONEとし、HALは周波数だけで選局する。AOSP SDK defaultの`streamIdType=STREAM_ID`かつ`streamId=INVALID_STREAM_ID(0xFFFF)`はselectorなしとして吸収する。CS110に実selectorが指定された場合は`INVALID_ARGUMENT`とする。

## BS/CS110 selector固定テスト
次の契約を単体テストで固定し、必要に応じて静的確認で補強すること。

- TISがBSの通常候補をbackendにかかわらず`STREAM_ID`のTSIDで生成し、HAL内部capabilityまたは相対slotを参照しないこと。
- earth_pt1 / DVB backendがBS `STREAM_ID 0..65534`をabsolute値として処理すること。
- px4 backendが`RELATIVE_STREAM_NUMBER 0..7`をlegacy `slot`へ直接渡すこと。
- px4 backendが`STREAM_ID 12..65534`をlegacy `slot`へ直接渡すこと。
- px4 backendがabsolute `STREAM_ID 0..11`を相対番号へ誤解せず、副作用なしの`UNAVAILABLE`とすること。
- frontend_px4が固定TSID→slot表またはTMCC由来の暗黙変換を持たず、TISがdriver名またはHAL内部capabilityで候補を分岐しないこと。
- TISがCS110 tune requestにstream selectorを付けないこと。
- HALがCS110の実selectorを`INVALID_ARGUMENT`とし、AOSP SDK defaultの`STREAM_ID / INVALID_STREAM_ID(0xFFFF)`だけをselectorなしとして吸収すること。

## px4_drv系のロック方法
px4_drv の AOSP frontend lock状態、status capability、callback遷移の公開契約は `tuner_hal/DESIGN_JA.md` の「px4_drv ロック 方針」を唯一の正本とする。

px4_drv / product-level invariant として、`PTX_SET_CHANNEL` は driver 内部の `ops->check_lock()` が当該要求の demodulator lock 成立を確認した場合だけ成功する。また、現行 userspace ABI には選局後の current demodulator lock を副作用なく読み戻す経路がない。read-only ABI と I/O failure 分離の追加条件は `future_work/r51/px4_demod_lock_status_readback_blocker.md` を参照し、AOSPへの具体的な状態写像は本書で重複定義しない。

## px4_drv系のchardev open方法
px4_drv の legacy chardev は同一 physical chardev endpoint の二重 open を許さないため、frontend_px4 の tune/control 経路とライブ TS read 経路は同一 endpoint を独立に `open()` してはならない。1つの backend owner が1つの device-open resource を所有し、control / read view を内部で分ける場合も別の device-open ownership を作らない。TS reader 準備失敗等を理由にした同一 endpoint の再 open fallback、別 path への open fallback、TS read 用の独立 open は禁止する。

この single-open / single-ownership 制約は px4 backend の ライブ視聴、EPG section 取得、AV filter、DVR/record path の前提である。owner resource から control / read view を導出する具体 API、helper 名、単体テストの内部形状は本書では固定しない。product default の実装規約は `tuner_hal2/CODE_CONVENTION.md` §18「px4 single-open backend 実装規約」を正本とする。

## TS packet 処理、section assembly、SI/EIT 意味解析 の責務境界

「TS解析」という曖昧語を単独で使わない。次の正式語に分ける。

- TS packet processing:
  188-byte TS packet、PID、adaptation フィールド、continuity counter、transport_scrambling_control、payload offset を扱う処理。担当は Tuner HAL とする。

- section assembly:
  TS packet payload から PSI/SI section payload を復元し、セクションフィルター へ配送する処理。担当は Tuner HAL とする。

- PES / AV / DVR delivery:
  PES、AV payload、record / DVR raw TS を HAL filter / DVR 契約に従って配送する処理。担当は Tuner HAL とする。ライブAVは製品アーキテクチャとして clear-memory non-passthrough `MediaEvent`だけを使用し、secure-memory、tunneled、AV passthrough は恒久的に採用しない。共有領域+`dataId`方式とイベント固有fd+`dataId`方式の両方を正式経路とする。共有handle未取得、使用権解放済み、共有領域空き不足の場合はイベント固有fd方式を使う。AV payloadを通常FMQ / EventFlagへ載せる経路、およびAV filterを他filterのsourceとする経路は実装しない。

- SI/EIT 意味解析:
  Tuner HAL から届いた section payload をもとに、PAT、PMT、CAT、SDT、NIT、BAT、EIT、descriptor を解釈し、サービス、event、EPG、CA情報 の 意味モデル を作る処理。担当は arib_si_engine_rs と TIS とする。

- TvProvider projection:
  意味モデル を `TvContract.Channels`、`TvContract.Programs`、`internal_provider_data` へ反映する処理。担当は TIS とする。

Tuner HAL を サービス検出、ARIB文字列、サービス メタデータ、CA情報、EPG 意味モデル の SSOT にしない。Tuner HAL は packet / filter / delivery の責務に限定し、意味解釈と TvProvider 反映は TIS 側に閉じる。

# ARIB文字/字幕のデコード
自前 ARIB文字 decoder(Rust実装)とlibaribcaption を改変せずを併用する。  
自前 ARIB文字 decoder は字幕以外の SI/EPG 文字列用(サービス名、プログラム名、番組詳細説明などなど)に限定し、字幕処理は TIS 側の字幕 path で libaribcaption のみを使う。libaribcaption は C API のみを使用し、独自 C/C++ 薄層 は書かず、Rust 側 safe ラッパー で所有権、cleanup、Result化、Rust モデル変換を行う。


# 実装方針

## CAS
CAS HALの実装はr52の正式リリース完了までプレースホルダーとする。CAS HALの実装が完了している前提を仮置きしてはならない。

ただし、AOSPのMediaCas/Tuner/IDescramblerの接続契約に沿うため、TISとTuner HALはr52のCAS HAL本実装へ接続できる境界をr51までに先行実装する。TISはECMセクションフィルター、MediaCas / CAS bridgeへの型付き配送、session lifecycle、実トークン取得時のdescrambler接続境界、未接続診断を実装し、B25用には加えてEMMセクションフィルターと`MediaCas.processEmm()`接続境界を実装する。B1経路ではEMM filterを起動せず`MediaCas.processEmm()`を呼ばない。ただし r51 本番経路では CAS HAL が 仮実装 であるため、実 CAS トークン が得られない場合は `setKeyToken()` を呼ばず、スクランブル サービスを CAS_UNAVAILABLE / video unavailable / 診断へ落とす。Tuner HALは`IDescrambler`、`setKeyToken()`、`addPid()` / `removePid()`、トークン lookup境界、未接続・bad トークン・expired トークン診断までを実装する。

本番TISは、仮トークンまたは診断専用トークンを`setKeyToken()`へ渡してはならない。仮トークンはTuner HAL単体テスト、fake CAS、診断注入専用であり、復号成功として扱ってはならない。

`setKeyToken()` に渡す byte array は、Tuner SDK API の制約に合わせて 1〜16 byte の opaque トークン に限定する。長い文字列形式の 仮トークン / 診断 トークン 名は、テスト名、ログ表示名、診断 dump のラベルに限定し、TIS から Tuner SDK API の `setKeyToken()` へ渡してはならない。

## 放送規格準拠
設計はAOSPの契約に準拠した上で、ロジックはARIBの規格に準拠すること。(ARIB.7zに格納されている)

## Tuner HAL側に置くB25デスクランブル処理
TS解析結果のSSOTをTIS側に置くため、TunerHALのIDescramblerを必ず実装する。  
録画後に後からデスクランブル可能な録画 TS を残すため、デスクランブル失敗時に scrambled TS packet を後段へ流す仕様とする。

## B25デスクランブル関連の r52 module 責務

### 前提

r52 の Tuner HAL descrambler 責務は、188 byte TS packet の payload offset 判定、`transport_scrambling_control` による odd/even key 選択、MULTI2 復号中核、復号成功時の scrambling_control 平文化、復号失敗時の scrambled TS pass-through、opaque key token を内部鍵資源へ解決する境界、descrambler 診断カウンター / dump とする。

### B25 / CAS / TIS の責務境界

B25 / CAS / TIS の責務は、AOSP Media CAS / Tuner の境界とARIB STD-B25の ECM / EMM / Kw / Ks / MULTI2 の役割分担に合わせ、次に固定する。

| 境界 | 責務 | 禁止事項 |
|---|---|---|
| TIS | CA descriptor / ECM / in-band EMM の取得、CA system 選択、`MediaCas` / session orchestration、ECM の `MediaCas.Session.processEcm()`、in-band EMM の `MediaCas.processEmm()` へのrouting、CASのtyped resultをservice-levelの視聴可否・UI状態へ反映する判断 | Kw / Ks、card protocol、MULTI2 algorithm を解釈・保持しない。生のKsを受け渡さない |
| CAS HAL / MediaCas plugin / vendor CAS layer | B25固有のECM / EMM処理、B-CAS / card I/O、CAS方式固有のentitlement validation、EMM / card処理によるKw管理、ECMをKwで処理したKsの導出。KsをTuner descramblerから参照可能な安全な内部鍵資源へ設定し、標準MediaCas session IDのbyte sequence（`MediaCas.Session.getSessionId()`）とその内部鍵資源の対応をvendor key bridgeで成立させる。このsession ID bytes自体をTuner key tokenのidentityとする | 188-byte TS packet payloadのMULTI2 descramblingをCAS側へ移さない。生のKw / KsをTISへ公開しない |
| CAS / vendor key bridge → TIS | `MediaCas.Session.getSessionId()` と同一byte sequenceのopaque key tokenだけをTISへ渡す。key slotの物理実装方式はvendor内部契約に閉じる | TIS向けに別形式のvendor-private tokenを発行しない。raw key materialまたはkey-slot内部表現をTIS / Binder契約へ露出しない |
| TIS → Tuner HAL | `MediaCas.Session.getSessionId()` と同一byte sequenceのopaque key tokenを`IDescrambler.setKeyToken(token)`へ接続し、対象PIDを`addPid()` / `removePid()`へ接続する | MediaCas session IDから別形式のtokenへTIS側で変換しない。Tuner HAL内部のkey table / key resourceを直接変更しない |
| Tuner HAL `IDescrambler` | opaque tokenから対応する内部鍵資源 / Ksを参照し、188-byte TS packet payloadのMULTI2 descrambling、odd / even key選択、payload offset判定、復号成功時の`transport_scrambling_control`平文化を担当する | ECM / EMM解釈、card I/O、CAS方式固有のentitlement validation、service-levelの視聴可否・UI判断を担当しない |

責務間のデータフローは次を正とする。

```text
                EMM
TS/Tuner ──────────────> TIS ──processEmm()──> MediaCas / CAS HAL / B-CAS
                                                  │
                                                  │ EMM処理
                                                  │ → Kwを獲得・保持
                                                  │
                ECM                               │
TS/Tuner ──────────────> TIS ──processEcm()──────>│
                                                  │
                                                  │ ECMをKwで処理
                                                  │ → Ksを獲得
                                                  │
                                                  │ key slotへKs設定
                                                  │ session ID bytesをkey slotへ登録
                                                  ▼
TIS <──────── opaque key token (= session ID bytes) ─ CAS/vendor key bridge
 │
 │ IDescrambler.setKeyToken(token)
 ▼
Tuner HAL
 │
 │ token → key slot → Ks
 │
 │ Ksを用いたMULTI2
 ▼
scrambled TS ───────────> clear TS
```

図中の`key slot`は、Tuner descramblerがopaque tokenで参照する安全な内部鍵資源を表す論理名である。AOSP Media CASはhardware key ladder / TEE等のvendor固有接続を標準化しないため、物理key slotの実装方式はCAS / vendor key bridge内部契約に閉じる。TISはKw / Ksを受け取らない。

本節は、Tuner HAL packet descrambler、CAS内部処理、TIS接着層の境界を一意に定める。TISはAndroid標準経路上のorchestrationとservice-level状態への写像を担当し、B25暗号アルゴリズムやカード処理を持たない。

### TIS が所有する接着責務

1. **CA情報 の サービス 単位管理**
   - Tuner HAL は PMT/CAT/SDT/ECM/EMM 等の section payload を Tuner SDK API の filter 経由で配送する。Tuner HAL は CA情報 / サービスメタデータ意味モデル の生成者またはSSOTにならない。
   - arib_si_engine_rs / TIS は、Tuner HAL から届いた PMT/CAT/SDT 等の section payload を解釈し、PMT/CAT の CA_descriptor 由来の CA_system_id、ECM PID、EMM PID、SDT 等に由来する free_CA_mode / scrambling flag、service_id、transport_stream_id、original_network_id を CA情報 / サービスメタデータ意味モデル として生成する。
   - 生成した CA情報 / サービス メタデータは、TIS の channel/session 内で サービス 単位に保持する。TvProvider の安定 key、診断情報、Tuner HAL の filter delivery 状態と混同しない。
   - 同一 service_id が別 TS/ONID に存在する反例で誤接続しないよう、サービス識別子は ONID/TSID/service_id を基本にする。

2. **ECM セクションフィルター の起動と MediaCas への配送**
   - 選局中 サービスの PMT から得た ECM PID に対して、Tuner SDK API で セクションフィルター を開く。
   - 取得した ECM section を、reflection ではなく型付きの `MediaCas.Session.processEcm()` 経由で CAS 側へ渡す。
   - TIS は ECM を解読しない。TIS は section を CAS へ配送し、CAS / vendor key bridge で成立した opaque token または typed failure を扱うだけにする。

3. **EMM セクションフィルター の起動と MediaCas への配送**
   - CAT から得た EMM PID をもとに、in-band EMM section を取得する。CAS plugin が vendor 固有経路で out-of-band EMM を取得する場合、そのネットワーク取得は CAS 側の責務とする。
   - 取得した in-band EMM は型付きの `MediaCas.processEmm()` 経由で CAS 側へ渡す。
   - TIS は EMM の権利更新、Kw 管理、カード応答解析を行わない。CAS方式固有の entitlement validation は CAS 側に閉じ、TIS はその typed result を service-level の視聴可否・UI状態へ写像する。

4. **MediaCas session lifecycle 管理**
   - channel tune / サービス change / PMT change / release に合わせて MediaCas session を open/close する。
   - ECM PID や CA_descriptor が変化した場合に、古い session/トークン を失効させ、新しい session/トークン へ切り替える。
   - session を サービス識別子 と結び付け、複数 サービス・複数 PID の混線を防ぐ。

5. **Tuner descrambler への トークン 接続**
   - MediaCas / CAS bridge から得た opaque トークン を、Tuner SDK API の descrambler に渡す。
   - `openDescrambler()`、`setKeyToken()`、`addPid()`、`removePid()` を reflection なしで呼ぶ。
   - TIS は raw Kw / Ks を扱わず、トークン を生成しない。
   - 本番TIS は 仮トークン または診断専用トークンを `setKeyToken()` へ渡さない。仮トークン は Tuner HAL 単体テスト、fake CAS、診断注入専用であり、復号成功として扱わない。

6. **PMT 変化に応じた descrambler PID 管理**
   - PMT から映像 PID、音声 PID、字幕 PID、データ PID などの scrambled ES を特定する。
   - 対象 PID を descrambler に add/remove する。
   - PMT version 変更、サービス 切替、ES 追加・削除に追従する。

7. **scrambled pass-through 録画の状態管理**
   - Tuner HAL が descramble 失敗時に scrambled TS packet を後段へ流す仕様を、TIS 側で「後段デスクランブル可能な録画 TS を残すための意図仕様」として扱う。
   - 視聴 path では 平文 映像が得られていない場合に `notifyVideoAvailable()` を出さない。
   - 録画 path では「scrambled 録画」「平文 録画」「復号失敗混在」などの状態を診断・メタデータとして区別する。

8. **CAS/descrambler 診断の統合表示・ログ化**
   - CAS 未接続、トークン 形式不正、トークン 未登録、key slot 失効、ECM 未取得、EMM 未取得、descrambler NO_KEY などを TIS の診断にまとめる。
   - Tuner HAL の descrambler dump と CAS/MediaCas の 状態 を対応付ける。
   - 実復号成功と 仮実装/診断 トークン を混同しない。

9. **CAS結果に応じた TIS session 状態通知**
   - CAS 側から未契約、カード未挿入、権利期限切れ、ECM 失敗などの typed result が通知された場合に、TIS session の video unavailable reason、診断ログ、UI 向け状態を更新する。
   - CAS方式固有の entitlement validation は CAS 側、CAS結果を service-level の視聴可否・UI状態へ写像する判断は TIS 側の責務とする。

10. **B25/CAS 統合の atest / fake CAS テスト**
    - fake MediaCas / 疑似トークン / fake descrambler を使い、ECM→トークン→setKeyToken→addPid の接続を atest で固定する。
    - 仮トークン が復号成功にならず、CAS bridge 未接続診断へ落ちることを固定する。
    - PMT 変更、ECM PID 変更、session close、サービス 切替、scrambled pass-through 録画を regression test 化する。

### TIS が所有しない責務

次は TIS が所有しない。

1. MULTI2 復号アルゴリズム本体
2. ECM の暗号解読・Ks 導出
3. EMM の権利更新処理
4. B-CAS / ACAS / スカパー系カード I/O
5. APDU 組み立て・カード応答解析
6. raw Kw / Ks の保持・配送
7. opaque トークン の本物発行
8. CAS方式固有の entitlement validation
9. libaribb25 型の一体型 TS→TS B25 pipeline
10. Tuner HAL の packet-level descrambler 処理

## B1デスクランブル関連の r52 module 責務

B1 の正式対応範囲は **ECM-only** とする。B1 のスクランブル解除成功に EMM を要求せず、TIS は B1 session のために EMM section filter を起動せず、`MediaCas.processEmm()` を呼ばない。CAS HAL / MediaCas plugin / vendor key bridge も B1 capability として EMM 処理を広告しない。

- TIS は PMT / CA descriptor から B1 の CA system と ECM PID を解決し、取得した ECM section を型付きの `MediaCas.Session.processEcm()` 経路へ配送する。
- B1 CAS 側は ECM-only の対応経路で成立した内部鍵資源と標準 MediaCas session ID bytes の対応を vendor key bridge で確立し、TIS はその opaque token だけを Tuner descrambler へ接続する。raw key material を TIS / Binder へ出さない。
- B1 では EMM に依存する activation / control information の取得、契約更新、権利更新を対応しない。それらを必要とする service / session を B1 対応成功として扱わない。
- CAT に EMM PID が存在しても B1 の必須入力、復号開始条件、復号成功条件にはしない。B1 session で EMM 未取得を failure reason にしない。
- B1 の統合試験は ECM → MediaCas session → opaque token → `setKeyToken()` → `addPid()` の接続を確認し、B1 経路から `MediaCas.processEmm()` が呼ばれないことを固定する。

この境界はリリース時点に依存する暫定扱いではなく、B1 capability の恒久契約である。

## 流用対象
ここでの流用とは、ロジックを参考にして真似して再実装することを指す。  
ゴールを満たすには、製品ロジックをC/C++で実装してはならないことに注意する。例外は、`libaribcaption`本体のリンク利用、およびFMQ/EventFlag、dma-buf/libdmabufheapなどAOSP native APIへ接続するための極薄shimだけである。libaribcaptionはC APIのみを使用し、独自C/C++ 薄層を書かない。Rust側に所有権、cleanup、Result化、Rust モデル変換を担うsafe ラッパーを置く。

- Tuner HALとデバイスファイルの界面...recisdbなどを参考にする
- arib_si_engine_rs及びTuner HALのsoft_demux系...mirakc,tsukumijima/libaribb25から、ノイズに対する頑健性が高くなる有用な実装を積極的にロジック流用し、Rust実装を作り上げる
- TIS の ARIB 字幕 track 表示経路...PMT の字幕 component から `TvTrackInfo.TYPE_SUBTITLE` を作り、`onSetCaptionEnabled()` と libaribcaption C API 経路を接続する。字幕 PES、trackメタデータ、表示経路は TIS 側の責務とし、BML / data broadcast 実行環境、双方向 UI、データ放送 UI は恒久対象外とする。
- TunerHALのIDescramblerB25デスクランブル処理...tsukumijima/libaribb25からロジック流用し、pure Rust実装を作り上げる。ただし、上の実装方針を優先するため、TS処理部分などは流用しない。
- CAS HAL...libyakisoba
- rec...EDCB


## コーディング規則の置き場所

プロジェクト全体に共通するコーディング規則は、tv 直下の `GLOBAL_CODE_CONVENTION.md` に置く。Rust 全般に適用する `panic` / `unwrap` / mutex汚染 / ワーカー / FFI / parser 境界の規則は、このファイルの Rust 節を正とする。

モジュール固有のコーディング規則は、各モジュール直下の `CODE_CONVENTION.md` に置く。no-`panic`、mutex汚染、一般worker / lock / callback、FFI / parser、テスト自己参照禁止など全moduleに共通する規則は`GLOBAL_CODE_CONVENTION.md`を正とする。product default Tuner HALのAIDL error mapping helper、typed entry、worker / callback / FMQ / source boundary等の現行実装規約は`tuner_hal2/CODE_CONVENTION.md`を正とする。`tuner_hal/CODE_CONVENTION.md`は旧参照実装固有の具体type / helper / module規約だけを保持し、`tuner_hal2`の規範として使用しない。

`DESIGN_JA.md` には設計判断を書き、コード記法・`panic` 禁止・ロック helper の使い方のような実装規約は `CODE_CONVENTION.md` に寄せる。状態遷移、戻り値、資源寿命、失敗時の閉鎖対象は `DESIGN_JA.md` を正とし、実装記法、禁止構文、helper 使用規則は `CODE_CONVENTION.md` を正とする。規則の重複記載で内容が割れた場合は、内容の分類に従って正とする文書へ集約し、他方を参照記載へ直す。

# リリース物のルール
指示なく、future_workディレクトリにアイテムを追加しないこと。  
いかなる場合もこの文書に登場しないモジュールを追加しないこと。  
tvディレクトリには、このファイル、README.md、AGENTS.md、GLOBAL_CODE_CONVENTION.md、タスク完了判定の実施方法.md、ARIB_SI_EPG_TvProvider投影方針.md以外のMarkdownを置かない。  
README.md は人間向けの入口文書とし、リポジトリ内の主要文書、モジュール、Codex 起動位置への案内だけを記載する。設計判断、状態遷移、戻り値、資源寿命、失敗時処理、完了条件、統合手順、変更履歴を定義してはならない。  
AGENTS.md は作業エージェント向けの入口文書とし、作業開始位置、LineageOS target 初期化、必読文書、最低禁止事項だけを記載する。設計判断、状態遷移、戻り値、資源寿命、失敗時処理、完了条件、統合手順、変更履歴を定義してはならない。  
詳細な完了判定、文書責務、設計判断、統合手順は、それぞれ `タスク完了判定の実施方法.md`、この `開発規則.md`、各モジュールの `DESIGN_JA.md`、各モジュールの `INTEGRATION.md` を正とする。  

実行可能権限がついたファイルがアーカイブに含まれないこと。ただし、render_vts_config.pyに限り、実行権限を付与して格納すること。  

`タスク完了判定の実施方法.md`に沿って、TODOが中途半端に止まっている状態ではないことを確認する。  
成果物の機械的チェック"だけ"を行うスクリプトファイルの格納を一切禁止する。  

各モジュールのフォルダが含んでも良いMarkdownは下記のファイル種別のみとする。それ以外のドキュメントを追加してはならない。  
丸括弧内の規定に沿わない記載が含まれてはならない  
- README_JA.md(このディレクトリにあるものを第三者が使い始めるために必要な情報「だけ」を記載する)
- CHANGELOG.md(必ず新しいものから降順に記載せよ。違反していたらリリース否認となるため並び替えよ。リリース時の報告とCHANGELOGの当該リリース項目は、「主な修正内容」ではなく、実際に変更したすべての修正内容を列挙する。未達を見落とさないため、helper追加・public到達経路・文書追従・テスト期待値更新・未実行検証を分けて記載する。)
- DESIGN_JA.md(このドキュメントに含まれていない設計方針、合意した設計判断だけを記載する)
  - 例外的に `tuner_hal2/DESIGN_JA.md` は、既存 `tuner_hal/DESIGN_JA.md` と同じ公開契約・状態遷移・戻り値・資源寿命、0-S-3Bの`WorkerRuntime` / `WorkerHandle` / `WorkerFailureClassifier`論理契約、および「scan / tune worker terminal mapping」「ワーカー終了契約」を再定義せず、`tuner_hal2` 固有の構造差分と対応境界だけを記載する。
  - `tuner_hal2/DESIGN_JA.md` に、README相当の使い始め説明、CODE_CONVENTION相当の実装記法・禁止構文、INTEGRATION相当のproduct統合手順、CHANGELOG相当の履歴、○×表相当の判定結果・未達理由を書いてはならない。
  - `tuner_hal2` の使い始め説明は `README_JA.md`、実装規約は `CODE_CONVENTION.md`、product統合手順は `INTEGRATION.md`、変更履歴は `CHANGELOG.md`、完了判定はアーカイブ外の○×表を正とし、同一内容を複数文書に重複配置しない。
- CODE_CONVENTION.md(そのモジュールに特化したコーディング規則だけを記載する)
- INTEGRATION.md(実機組込時のMakefileへの追加方法、APEXの作成方法、priv-app / permission / product feature / boot receiver / product integration 条件を第三者であるユーザーに説明するためのドキュメント。Tuner HAL、CAS HAL、TISに限って許可する)

### 恒久文書・変更履歴・互換残骸の収録規則

一時レビュー用Markdown、一時変更履歴、レビュー返信下書き、移送監査表などの作業用文書をリリースアーカイブに同梱してはならない。作業中の内容を恒久化する場合は、設計判断は各モジュールの`DESIGN_JA.md`、実装規約は`CODE_CONVENTION.md`、product統合手順は`INTEGRATION.md`、変更履歴は`CHANGELOG.md`へ統合し、元の一時文書を削除する。

恒久的な変更履歴の正本は各モジュールの`README_JA.md`が案内する当該モジュールの`CHANGELOG.md`だけとし、同一変更履歴を別名Markdown、補助履歴ファイル、ソースコメントへ重複保持しない。

本プロジェクトの未公開版相互の後方互換だけを目的とするalias、互換field、旧APIは、現行のAOSP公開契約または採用済みの外部互換契約が要求しない限り、非公開化して残さず削除する。

CHANGELOG.md以外の場所(ソースコードも含む)に、過去に下された設計判断以外の古い情報や編集履歴っぽい情報を残してはならない。リリース前に必ず削除が必要。  
CHANGELOG.mdとログメッセージ出力以外は、コード内のコメントも含め日本語のみを使うこと。ルー語は望ましくないがやむを得ない場合は慣用上問題ない場合においては許可する。  
各モジュールにおいて、atestで実行可能な単体テスト群を必ず用意すること。  

テストによる本体ソース自己参照・文字列検査の禁止と、設定・文書整合確認での補助的な`include_str!()`利用条件は`GLOBAL_CODE_CONVENTION.md`のRust test規約を正とする。  

# r51の受け入れ条件
フォルダ構成はsoongルートに直接tar -xzfで適用可能な形を想定し、vendor/maleicacid/tv/配下に成果物が入っていること。  

全体的なゴール、モジュールごとのゴール、リリース物のルール、設計方針、実装方針をすべて満たしていること。

## TIS / arib_si_engine_rs provider-data・状態機械・失敗処理の開発規則

TIS と `arib_si_engine_rs` の境界で扱う TvProvider 内部データ、診断データ、snapshot transaction、executor / ロック、setup、retry は以下を開発規則として固定する。この節は TIS と `arib_si_engine_rs` の DESIGN_JA.md より上位の横断規則であり、各 DESIGN_JA.md はこの節と矛盾してはならない。


### provider-data 受け渡し境界

- TIS が JNI へ渡す JSON は、保存形式ではなく Rust serde 型へ値を渡すための受け渡し用形式とする。
- `Programs.COLUMN_INTERNAL_PROVIDER_DATA` / `Channels.COLUMN_INTERNAL_PROVIDER_DATA` に保存される JSON、schema、正規化、識別子抽出、切り詰め診断は Rust provider-data serde 構造体を正とする。
- TIS は保存用 provider-data JSON を直接生成してはならない。TIS が受け渡し用 JSON を組み立てる場合でも、型、必須項目、欠落時の扱い、旧形式拒否、値域検査は Rust 側 serde 型に従わせる。
- 必須項目欠落を `0`、`false`、`jpn`、`UNKNOWN`、空文字などで補って provider-data を成立させてはならない。
- `DescriptorDiagnosticV1` は Rust 生成の正規 JSON を透過保持し、TIS が項目ごとに再構築してはならない。

### provider-data / 診断情報の SSOT

- `Programs.COLUMN_INTERNAL_PROVIDER_DATA` と `Channels.COLUMN_INTERNAL_PROVIDER_DATA` の新規書き込み正形式は JSON v1 のみとする。r50 以前の `;` 区切り key-value 形式、旧 flat provider-data、旧 provider-data 断片を読み取り互換入力として残してはならない。r50以前の旧provider-dataはreleaseに依存せず恒久的にmigration対象とせず、既存端末ではDB再構築またはsetup再実行でJSON v1を再生成する。
- provider-data JSON v1 の構造、canonical encode、正規化、安定キー抽出は `arib_si_engine_rs` の Rust `serde` struct を SSOT とする。`arib_si_engine_rs/schema/program_provider_data_v1.schema.json` は top-level と nested オブジェクト の双方で required 最小フィールド と `additionalProperties: true` を併用し、固定済み フィールドを検証しながら ARIB descriptor 拡張を保持できる形にする。Channel provider-data は `schema="maleicacid.tv.channel"` / `schemaVersion=1` の JSON v1 とし、`arib_si_engine_rs/schema/channel_provider_data_v1.schema.json` を正とする。
- Kotlin TIS は provider-data JSON を手書き構築しない。Kotlin は Rust JNI の build / 正規化 / key extraction API を通じて、TvProvider へ書く UTF-8 JSON bytes を得る。provider-data単体のdigestまたはsignatureは生成しない。同一process内の重複書き込み抑止には、provider-data bytesを含むTvProvider行全体のpublish fingerprintだけを使用する。現在番組選択の診断は process-local に保持し、provider-data へ永続化しない。
- 記述子診断情報は Rust parser が生成する `DescriptorDiagnosticV1` を正とする。TIS はこれを別 schema へ変換してはならない。未対応の視聴年齢制限は `ratings[]` に構造化保存し、必要な補足だけ `diagnostics.publishDiagnostics[]` に warning として保存する。unknown key は無言で破棄せず、既存 JSON v1 の読み取り時は保持可能とし、新規 canonical 出力では既知 schema フィールドまたは `diagnostics.rawProviderDataExtensions[]` へ正規化する。
- program stable key は `ONID / TSID / SID / event_id` のみで構成し、start/end/duration を含めてはならない。start/end/duration は表示・検索・廃止行削除 用の timing data として provider-data の `timing` オブジェクト と TvProvider 標準列に保持する。

### provider-data サイズ上限

- provider-data 全体は 16 KiB を soft limit、32 KiB を hard limit とする。
- hard limit 超過時は identity / timing / CAS state / コンテンツレーティングを保持し、診断情報と長文補助情報を優先的に truncate する。
- 記述子診断情報は program あたり最大 16 件、publish 診断情報は program あたり最大 16 件、`rawPrefixHex` は最大 64 bytes、診断 `message` は最大 256 chars、`extendedItems` は最大 32 件、long description は最大 4096 chars、一般文字列 フィールド は最大 1024 chars とする。
- truncate が発生した場合、`DIAGNOSTICS_TRUNCATED` または `PROVIDER_DATA_TRUNCATED` 診断と dropped count を必ず保存する。


### 廃止経路・互換専用シンボルの禁止

- `arib_si_engine_rs` は Android canonical genre の写像表を所有せず、SI event DTO に旧 `canonicalGenres` フィールドを出力してはならない。`Programs.COLUMN_CANONICAL_GENRE` の値は TIS が明示写像表に基づいて決定する。provider-data に保持する canonical genre 投影結果も TIS が決定した値だけとする。
- `nativeGetEventCount()` と `nativeGetEvent*` indexed JNI getter 群は廃止する。通常境界は `nativeSnapshotBulkJson()` と provider-data JNI API のみとし、未使用 private external 宣言、空返却だけの互換シンボル、呼び出し不能な廃止予定 path をリリース物へ残してはならない。

- r50 以前の provider-data 互換入力経路は禁止する。`;` 区切り key-value parser、旧 flat provider-data parser、旧 DTO 断片を通常経路・復元経路・診断経路・test fixture に残してはならない。

### transaction DTO 境界

- 本番経路で `snapshotEvents()`、`snapshotPublishabilityDiagnostics()`、`takeEpgUpdateWindows()` 等の複数 公開snapshot を 呼び出し側 側で合成してはならない。
- 廃止 snapshot wrapper は本番経路・公開通常境界・product build に残してはならない。テスト専用に必要な入口は test source または test-only 可視性に隔離し、本番 APK / JNI API / release API から参照不能にする。
- Program publish 用には `takeProgramPublishSnapshot()` 相当の 単一transaction DTO を使い、events / 更新区間s / publishability / 記述子診断情報 / parser 診断情報を同一ロック / 同一 native state から取得する。
- サービス登録用には `serviceRegistrationSnapshot()` 相当の DTO を使い、services / actual transports / publishability / 診断情報を同一世代に閉じる。
- CAS検出用には `casDiscoverySnapshot()` 相当の DTO を使い、services / CA情報 / PMT PID / 診断情報を同一世代に閉じる。CAT EMM PID は B25 用の任意fieldとして同じsnapshotに含めてよいが、B1の成立条件・session開始条件・復号成功条件に使用しない。
- drain型state は transaction DTO API 内でのみ消費し、別snapshot と後から合成しない。LiveSession の現在番組判定、視聴年齢制限判定、映像メタデータ補完のように updateWindows を消費してはならない read-only 参照は `programStateSnapshot()` を使い、drain 型 state を返してはならない。

### executor / ロック境界

- `MaleicacidLiveSession` は session-level single-owner executor に状態遷移を閉じる。current サービス、playback 署名、track state、unblock state、latest video メタデータ、ProgramPublishCoordinator へのアクセスを同一境界に置く。
- `PlaybackPipeline` は playback-level single-owner executor に public state mutation を閉じる。`setSurface()`、`setVolume()`、`start()`、`switchAudio()`、`stop()`、`release()` は executor 上で実行し、呼び出し元スレッドで mutable state を直接変更しない。
- `ChannelScanManager` は scan executor と scan generation トークン を持ち、cancel / cleanup は対象 generation にだけ作用する。stale cleanup が後続 scan の `running` や controller / engine を変更してはならない。
- `AribSiEngine` Kotlin ラッパー は single ロック、Rust parser state は `Mutex<ParserState>` に閉じる。native parser ロックを持ったまま TvProvider 書き込みや Android framework コールバック を呼ばない。
- public コールバック は generation / released flag を確認して owner executor に enqueue し、owner executor 上で再度 generation を確認してから state mutation する。

### SetupActivity 保護

- `SetupActivity.onCreate()` は privileged scan を自動開始してはならない。
- setup scan 開始前に、正規 setup flow の `inputId` が自 TIS の inputId と一致することを検証する。不一致または欠落時に 代替処理 inputId で scan へ進んではならない。
- scan 開始は inputId 検証済みのユーザー操作、または検証済み setup request に限定する。
- product 側で システムTVアプリ に grant 可能な場合は 署名 / privileged permission により SetupActivity を保護する。permission grant ができない target では、少なくとも自動 scan 禁止、inputId 検証、ユーザー操作開始を必須とする。
- `ChannelScanManager` の state は `purpose` と `scanGeneration` を持つ。SetupActivity は自分が開始した `SETUP_SCAN` generation の `Completed` のみで `RESULT_OK` にする。過去の Completed や boot/background maintenance の Completed で finish してはならない。

### retry / backoff

- TvProvider publish retry key は `ServiceKey + updateWindow + failureClass` とする。
- retry backoff は 1分、5分、15分、60分とし、最大 interval は 60分、jitter は ±20%、最大 attempts は 10、保持期間は 24時間または次回正常 snapshot までとする。
- provider failure 時は 廃止行削除、署名 update、pending 平文 に進まず、window を retry queue に残す。
- boot EPG pending は TvProvider unavailable、ライブセッション active、scan active、all candidate failure、publish failure では 平文 しない。successful publish が 1件以上ある場合だけ 平文 できる。
- boot/background retry は ライブセッション 中および setup scan 中に開始せず defer する。

### ARIB malformed SI / descriptor の扱い

- ARIB descriptor は `descriptor_length`、descriptor 内部 length、loop 単位、fragment sequence が妥当な場合だけ TvProvider 標準列および provider-data の正常フィールドに採用する。
- length 不整合、余剰 byte、fragment 欠落、`descriptor_number` 重複、`last_descriptor_number` 不一致、必須フィールド 不足は 不正 descriptor とし、title / description / genre / audio / linkage / イベントグループ 等の正常フィールドには採用しない。
- 不正 descriptor は parser を停止させず、`DescriptorDiagnosticV1` に tag、offset、declaredLength、actualRemainingLength、parseStatus、rawPrefixHex、section scope を保持する。
- EIT event fixed フィールド、start_time BCD、duration BCD、descriptor_loop_length が不正な event を含む section は、既存 event 削除用の authoritative valid-event-set として扱ってはならない。
- obsolete Programs delete の根拠に使える EIT section / table snapshot は `deletionAuthoritative=true` のものに限定する。不正 event がある場合は既存正常 event を保持し、診断に記録する。

### Tuner SDK コールバック 入力上限

- TIS の PSI/SI section path は allocation 前に `SectionEvent.dataLength` を検証する。`MAX_SECTION_BYTES` は 4096 bytes とし、`dataLength` が 1..4096 の範囲にある場合だけ ByteArray 確保と parser 投入を許可する。
- section read size 不一致、0 length、負値相当、4096 bytes 超過は parser、CAS、Program publish に渡さず 診断カウンター に記録する。
- `MediaEvent` payloadは`offset >= 0`、`dataLength > 0`、加算overflowなし、`offset + dataLength <= mapped buffer capacity`を満たす場合だけdecoder queueへ渡す。
- sample byte上限は固定値にせず、TISでは同一`ProductProfile`のcodec、対象decoder/device、AOSP Tuner media filter eventの最大正常range、header収集量、allocator上限、実機最悪値を根拠に`TisPlaybackBudgetSnapshot.singleEventLimitBytes`をofflineで独立導出し、AV filter開始前に固定する。TISはHAL内部`CapabilitySnapshot`、`avMaxEventBytes`、`avPerFilterLiveBytes`、`avRuntimeBudgetBytes`をruntime参照・公開・複製しない。
- HAL側の`ProductProfile.avMaxEventBytes`とTIS側の`TisPlaybackBudgetSnapshot.singleEventLimitBytes`は同一`ProductProfile`の根拠から各層で独立にoffline導出し、対応宣言するcodec/profileの正常なmedia event rangeを双方が収容できることをproduct-level invariantとして静的に検証する。いずれかを正の有限値として検証できないprofileはAV対応を宣言せず、私的なTIS→HAL capability経路で補償しない。
- clear-memory eventの共有領域が利用不能な場合はHALのevent-local fd方式を使用できる。両方式の実サイズ予約が成立しない場合だけ当該eventを診断付きで破棄し、継続不能ならTISは`notifyVideoUnavailable()`へ接続する。TISはcodec別access-unit再構成を理由に追加のreassembly bufferを設けない。

### Direct Boot drain / ライブセッション 優先

- `MaleicacidTvInputService.onCreate()` は Direct Boot pending drain、boot EPG sync、background maintenance を開始してはならない。
- Boot EPG sync / background maintenance は BootReceiver、UserUnlockReceiver、または明示的な maintenance scheduler からのみ起動する。
- Boot EPG sync / background maintenance の開始条件は `activeLiveSessionCount == 0`、`sessionCreationInProgress == false`、`setupScanRunning == false`、`playbackPipelineRunning == false`、`scanManager running == false` をすべて満たすこととする。
- ライブセッション 作成要求が来た時点で boot/background task が未開始なら defer する。boot/background task が既に running の場合、r51 では boot/background task を cancel/defer し ライブ tune を優先する。

### TvProvider 必須問い合わせ / null cursor

- TvProvider query は 必須問い合わせ と 任意問い合わせ を区別する。チャンネル・番組の追加または更新、廃止行削除、既存チャンネル・番組検索、provider-data代替参照、Direct Boot準備完了 判定に使う query は 必須問い合わせ とする。
- 必須問い合わせ で `ContentResolver.query()` が null cursor を返した場合は `TvProviderQueryFailure` とする。null cursor を empty result とみなしてはならない。
- `TvProviderQueryFailure` が発生した サービス/window では channel insert、program insert/update、廃止行削除、署名キャッシュ更新、Direct Boot保留解除 に進まず、再試行区間 を保持する。
