## リリース段階ごとの到達点

本プロジェクトでは、AOSP標準の責務分担に従い、Tuner HAL、TIS、arib_si_engine_rs、CAS plugin、rec、epgcollection の責務を分離する。

最終製品ゴールは、AOSP標準の責務分担および契約（構成・インターフェース・実装などの仕様）に完全に準拠し、対象範囲の機能がすべて実装済みであることである。ただし、各リリースの完了判定では、そのリリースで **表明または有効化した機能範囲** について、AOSP の API 契約、責務分担、エラー契約、状態遷移、実動作が一致していることを完了条件とする。未実装機能を成功扱いにしてはならず、未対応機能は 能力情報に出さない、または 公開APIで `UNAVAILABLE` / `INVALID_ARGUMENT` / `INVALID_STATE` 等として明示的に拒否する。


AOSP標準の `packages/apps/TV` の Live TV / Reference TV app から、TvInputManager / TvInputService / Tuner SDK API 経由で視聴できることを確認対象とする。TIS は Tuner HAL binder を直接叩いてはならない。

ARIB放送規格および日本での放送運用に準拠することを、AOSP標準準拠とは別の完了条件として扱う。

ARIB適合性では、製品が準拠すべき規範対象と、レビューで適合性を確認するための検証証拠を分離する。規範対象は、製品scopeに適用される現行ARIB日本語原文とし、レビュー環境の会員資格、購入可否、ネットワークアクセスによって変更しない。ARIB公式英語版は原文の翻訳として扱い、原文との内容・表現等に差異がある場合は原文を優先する。

検証証拠は、対象条項の現行日本語原文を取得できる場合はその原文を用いる。会員認証または有償入手等により現行日本語原文をレビュー環境から取得できない場合は、ARIB公式英語版本文を条項確認の証拠として使用してよく、取得可能な日本語旧版より新しい公式英語版がある場合もその英語版を使用してよい。ただし、使用する英語版が現行日本語原文と同版でない場合は、未取得の現行原文との差分を未証明として明記し、その英語版だけを根拠に現行最新版への完全適合を確認済みとは扱わない。版一覧、改定概要、規格概要は版差分と適用範囲の確認には用いてよいが、未取得本文の具体的な条項内容を推測する根拠にはしない。

製品ライブAVのアーキテクチャは、AOSP Tuner framework の non-passthrough clear-memory ES path に固定する。TIS は `MediaEvent.getLinearBlock()` から MediaCodec の linear-block input へ渡す経路を使用し、secure-memory handle、tunneled playback、platform passthrough を製品のライブ再生能力として広告・使用しない。この選択はリリース時点に依存する暫定scopeではなく、製品アーキテクチャの恒久不変条件である。r52 の B25 / B1 対応も、Tuner descrambler 後の clear ES をこの経路へ接続できるサービスをライブ視聴成功の対象とし、secure media path を必須とするサービスを成功対応として表明しない。

製品レベルの安定性として、zapping、低品質信号の受信、単一TS複数サービス、解像度可変、スクランブル有無が小刻みに切り替わる stream、長時間動作、close/reopen、stop/start、再選局を確認対象に含める。

### モジュールと言語境界

- `tis`:
  - TvInputService、setup、channel 登録、ライブセッション、TvProvider 連携を担当する。
  - TIS の TS packet / section の低レベル解析以外を Kotlin のみで実装する。すなわちTvInputService、setup、channel登録、ライブセッション、TvProvider連携、Android API接着層は Kotlin で実装する。
  - TS/SI/EIT などの構造化解析は arib_si_engine_rs に置く。
  - Tuner HAL には Tuner SDK API 経由でアクセスする。
  - 例外として、ARIB字幕 path の libaribcaption C API 接続は Rust JNI boundary + 安全なRustラッパー を許可する。

- `arib_si_engine_rs`:
  - Tuner HAL から Tuner SDK API / TIS 経由で渡された PSI/SI/EIT section payload を入力として、PAT/PMT/CAT/SDT/NIT/BAT/EIT/descriptor の 意味解析 を行う。
  - raw TS packet processing、PID filter、section assembly、section delivery は Tuner HAL の責務であり、arib_si_engine_rs には実装しない。
  - 字幕処理と libaribcaption ラッパー を持ち込んではならない。
  - 字幕本文処理は TIS 側の字幕 path に閉じる。

- `tuner_hal2`:
  - Tuner HAL の公開契約・capability・状態遷移・資源寿命の設計正本はリポジトリ直下の `TUNER_HAL_DESIGN_JA.md` とする。
  - r50ee5以降の product default Tuner HAL service とし、`earth_pt1`（Linux Kernel builtin）と `px4_drv`（kazuki0824/px4_drv にある DDK 準拠チューナードライバ）向けの Tuner HAL 実装責務を一意に持つ。
  - `px4_drv` と `earth_pt1` は tune モデルが異なるため、選局候補表の SSOT は本書に置く。製品 scan 候補表の保持と実行時候補生成は TIS が担当し、Tuner HAL 内部では canonical frequency model と backend 変換を分離する。
  - FMQ / dma_heap との界面を除き Rust で実装する。例外として、host側のVTS設定用CLIツール（そのCLIプロセス内で使用するprofile生成、地域resolver、VTS XML compiler / validatorを含む）に限りPython実装を許可する。このPython環境は`uv`で作成・依存管理・実行することを前提とし、HAL runtime、AIDL実装、capability / resourceの規範正本、device-side helperその他の製品実装にPythonを使用してはならない。
  - r51 で対応宣言する `ITuner` / `IFrontend` / `IDemux` / `IFilter` / `IDvr` / `IDescrambler` / `ILnb` 面は、成功 no-op を残さず `TUNER_HAL_DESIGN_JA.md` のAOSP公開契約に従って実装する。ただしLNBについては、本書「LNB部分能力公開の製品例外」に定義するproduct-level例外を適用する。
  - `android.hardware.tv.tuner.ITuner/default` を登録する実体は `tuner_hal2` だけとする。

- `cas_plugin`:
  - `cas_plugin/DESIGN_JA.md` をMaleicacid CAS pluginの設計正本とする。
  - `android.hardware.cas.IMediaCasService/default` はAOSP標準MediaCasServiceを使用し、Maleicacid独自serviceを製品完成形として実装しない。
  - MaleicacidはAOSP `android::CasFactory` / `android::CasPlugin` C++ ABIに従うloadable vendor pluginを実装し、entry point `createCasFactory()` はC linkageとする。
  - YakisobaとSmartCardは同一B25 plugin内部のbackendとし、最初に`yakisoba_only`を成立させる。
  - B25/B1のpacket descrambleはTuner HAL `IDescrambler`が所有し、CAS pluginへ188-byte TS packet復号を移さない。

### 設計文書の責務分担

設計情報は、次表の所有文書だけを規範正本とする。別文書に同じ事実が必要な場合は正本を参照し、規範値・状態遷移・製品判断を独立に再定義しない。既存の越境記述を是正する場合は、情報を削除するのではなく所有文書へ移し、移動元を参照へ置換する。移動前に存在した規範情報の意味集合が移動後にも全て残ることを受け入れ条件とする。

| 文書 | 規範として所有する内容 | 所有しない内容 |
|---|---|---|
| `開発規則.md` | リリース到達点、製品全体または複数moduleにまたがるscope、製品scan候補の規範値、module間責務、product-level invariant | Tuner HAL個別APIの戻り値・状態遷移・VTS profile詳細、TIS内部runtime状態、PSI/SI parser内部意味モデル |
| `TUNER_HAL_DESIGN_JA.md` | AOSP Tuner HAL公開契約、capability/VTS境界、公開状態・戻り値・寿命・確定点・rollback/cleanup、TS/section/PES/AV伝送契約 | `tuner_hal2`の現在実装位置・移行進捗、TISのchannel登録/TvProvider判断、PSI/SI意味解析 |
| `tuner_hal2/DESIGN_JA.md` | `TUNER_HAL_DESIGN_JA.md`を実装へ接続する論理責務、依存方向、実装owner/anchor | 公開AIDLの規範値・状態遷移・戻り値・確定点・rollback/cleanupの独自定義 |
| `tuner_hal2/INTEGRATION.md` | product package、VINTF/init/ueventd/sepolicy、静的VTS設定の配置などproduct統合 | filter/section/PES/DVR等のruntime意味論、TIS/SI engineの実行時判断 |
| `cas_plugin/DESIGN_JA.md` | AOSP MediaCasService / vendor CasPlugin境界、B25/B1 capability、plugin/session lifecycle、backend ownership、ECM/EMM、session/tokenと動的鍵状態の対応・更新・失効、token寿命、revoke/teardown | Tuner HAL packet descramble公開契約、TIS runtime、製品release到達点 |
| `tis/DESIGN_JA.md` | TvInputService runtime、scan実行、channel登録、TvProvider利用、ライブ視聴、Android APIへの写像 | HAL binder契約、driver固有ABI、PSI/SI構文・descriptor意味解析そのもの |
| `arib_si_engine_rs/DESIGN_JA.md` | PSI/SI/EIT/descriptorの構文・意味解析、意味object、parser診断、provider-data schema/canonical encode | Android channel登録可否、TvProvider列への最終投影判断、製品リリースの収集scope、HAL状態遷移 |
| `ARIB_SI_EPG_TvProvider投影方針.md` | ARIB意味objectからTvProvider標準列/internal_provider_dataへの投影判断 | parser構文、TIS runtime、HAL公開契約 |
| `GLOBAL_CODE_CONVENTION.md` | 全moduleに共通する実装規約、禁止構文、一般worker / lock / FFI / parser / test規約 | module固有のowner / helper / entry、公開状態遷移、戻り値、capability、資源寿命 |
| `tuner_hal2/CODE_CONVENTION.md` | product default Tuner HALの実装規約、禁止構造、typed entry / helper使用規則、静的検査観点 | 公開状態遷移、戻り値、capability、資源寿命、transaction意味論の独自定義 |

`TUNER_HAL_DESIGN_JA.md` が正本として持つ公開transactionのphase order、確定点、rollback / cleanupと、`tuner_hal2/DESIGN_JA.md` が正本として持つ実装owner、実装anchor、禁止入口は一体の設計契約として扱う。これらの所有者、順序、確定点、巻き戻し、禁止入口のいずれかを変更する場合は設計変更として扱う。

#### LNB部分能力公開の製品例外

本製品は `earth_pt1` / `px4_drv` のLNBについて、Android 14 CTSのLNB試験合格よりも、hardware / driverで実処理可能と確認できたLNB operation / valueを公開することを優先する。このproduct-level判断は、上記の「対応宣言する公開面をAOSP契約どおりに実装する」という一般則に対するLNB限定の明示的例外である。

この例外を適用した部分LNB endpointはAndroid 14 CTSのLNB試験に加え、Android 14 AIDL Tuner VTSのLNB live試験でも失敗し得るため、本製品は当該LNB面についてCTS/VTS合格およびCDD適合を宣言しない。未対応operationを成功no-op、擬似成功、callback echoで実装済みに見せることも、公開済みLNBをVTS XML/profileから到達不能にして検査を回避することも禁止する。

この節が所有するのは「部分LNB能力を公開し、CTS/VTS/CDD compatibility deltaを既知として受容する」という製品判断だけである。公開endpointの選定条件、operation / valueごとの成功・`UNAVAILABLE`・`INVALID_ARGUMENT`、資源寿命、callback、rollback / cleanup等のTuner HAL公開契約は`TUNER_HAL_DESIGN_JA.md`を正とする。理由、CTS/VTS側の具体的試験内容、既知差分、再評価条件の記録は`future_work/not_planned/tuner_lnb_partial_hardware_cts_mismatch.md`を参照する。
#### STD-B25 Part 1 §4.9 最小鍵組容量の製品適合方針

STD-B25 Part 1 §4.9 が受信機システムに要求する最小鍵組容量は、本製品全体として恒久的に適合対象外とし、同条項への適合を宣言しない。`StdB25DecodeCapability` に記録する限定したTS payload decode能力、実鍵組数、実PID数を、同条項への適合、Part 1 CAS-R全体への適合、またはSTD-B25全面準拠の宣言へ読み替えてはならない。

規範値の証拠は[STD-B25 6.7-E1](https://www.arib.or.jp/english/html/overview/doc/6-STD-B25v6_7-E1.pdf) Part 1 §4.9（本文147頁）のチューナーごとの偶数・奇数1鍵組以上であり、従来の「8鍵組」は誤記として撤回する。現行日本語7.0の同条項との差分は未証明であり、適合を宣言しない製品方針だけから実容量不足という事実を断定しない。

この節が所有するのは上記product-level判断だけである。`StdB25DecodeCapability`、`DescramblerCapacityPool`、個別APIの受付・解放・失敗時契約は `TUNER_HAL_DESIGN_JA.md` を正とする。

#### 製品 scan 候補の規範値

CATV候補表は C13〜C63 に固定する。MID band は C13〜C22、SHB band は C23〜C63 とし、中心周波数は ARIB STD-B21 5.12-E2 Appendix 10 Table 10-3・Table 10-4 の `+1/7 MHz` オフセット込みで保持する。C22 は `167 + 1/7 MHz`、C23 は `225 + 1/7 MHz` であり、C21からC22、C22からC23は単純な6MHz連続として計算しない。

VHF 1〜12ch は開発規則.mdで恒久的にスコープ外であり、TISのCATV候補表、地上波候補表、共同受信候補表に追加してはならない。

BSのsetup/rescanは、TISがAOSP `Tuner.getAvailableFrontendInfos()`でISDB-S frontendを列挙し、公開`FrontendInfo.statusCapabilities`に`FrontendStatus.FRONTEND_STATUS_TYPE_STREAM_IDS`を持つfrontendを先に選択候補とする。TISはdriver名、frontend IDの数値規則、HAL内部backend種別、runtime errorからbackend種別または能力を推測してはならない。利用するfrontendはscan開始前に`Tuner.applyFrontend()`で明示的に確保する。

`FRONTEND_STATUS_TYPE_STREAM_IDS`を持つfrontendを確保できた場合は、そのfrontendを保持したままBS物理RFごとに`Tuner.scan()`を実行し、`ScanCallback.onInputStreamIdsReported()`で得たcurrent stream IDをIF周波数+typed `STREAM_ID`のexplicit tune候補へ展開する。 選択したfrontendはBS setup/rescanの候補source lifetime中保持し、RFごとの`cancelScanning()`では解放しない。explicit tuneと次RFのdynamic scanを同じfrontend lease上で継続し、RF境界でTRMの再選択へ戻してはならない。公開capabilityを持つfrontendが存在しても資源競合で確保できない場合は次の同capability frontendを試し、それらを確保できない場合に限り、`FRONTEND_STATUS_TYPE_STREAM_IDS`を持たないISDB-S frontendを`applyFrontend()`で確保できることを確認してTISの固定RF→absolute TSID候補表を使用する。候補sourceは最初のBS RFをscanする前に確定し、dynamic scanの失敗、timeout、空報告、`Tuner.RESULT_UNAVAILABLE`を理由に同一scan中で固定表へ切り替えてはならない。

固定TSID候補表は、BS01=`16400,16401,16402`、BS03=`16432,17969,17970`、BS05=`17488,17489`、BS09=`16528,16530`、BS13=`16592,16593,18130`、BS15=`16625,16626,18675`、BS19=`18224,18225,18226,18227`、BS21=`18256,18257,18258`、BS23=`18288,18801,18803`とし、BS07・BS11・BS17は候補を持たない。表は選局候補だけに使用し、受信後のservice identityのauthorityにはしない。channel登録するONID/TSID/SIDは実際に受信したPAT/NIT/SDT等のPSI/SIを正とする。表の更新はrepositoryの通常変更として履歴管理し、runtimeで複数revisionを切り替える独立version状態を設けない。Tuner HALはselector kindを保持して各backend ABIへ写像し、px4の相対slot表またはlegacy数値域をTISへ複製してはならない。

### r51 の到達点

r51 は、非スクランブル 平文 放送を対象に、AOSP標準経路で Live TV 視聴と Tuner HAL 基本機能を成立させるリリースである。

r51 で必須とする範囲は次のとおり。

- Tuner HAL:
  - `ITuner` / `IFrontend` / `IDemux` / `IFilter` / `IDvr` / `IDescrambler` / `ILnb` のうち、r51 で advertise / 対応宣言する機能範囲を AOSP 契約どおりに実装する。ただしLNBについては、本書「LNB部分能力公開の製品例外」に定義するproduct-level例外を適用する。
  - ISDB-T、BS、CS110 の explicit tune を対象にする。
  - 製品Japanese scan planはTISが所有してexplicit tune candidateを生成し、Tuner HALはproduct-specific Japanese scan planを生成しない。これはreleaseに依存しないmodule責務の恒久不変条件とする。
  - 平文ライブ視聴 AV path、section / PES / record / DVR path、DVR playback path を r51 対象にする。
  - `IDescrambler` API 境界、`setKeyToken()`、`addPid()` / `removePid()`、トークン lookup 境界、未接続・bad トークン・expired トークン 診断を実装する。
  - CAS plugin 仮実装 のまま 本番経路のスクランブル解除成功 を 対応宣言しない。
  - TS-only を r51 の対象とし、MMT / TLV / ISDB-S3 / B61 / 4K 放送は恒久的に対象外とする。

- TIS:
  - `TvInputService` として システムTVアプリ から呼ばれる。
  - Tuner HAL には HAL binder 直叩きではなく Tuner SDK API 経由でアクセスする。
  - setup / scan / channel 登録 / ライブセッション / 平文視聴 / 最低限の Programs 登録を実装する。
  - content genre、series、episode、free_CA_mode、音声言語、視聴年齢制限など、r51で取得する放送factのTvProvider標準列への具体投影は `ARIB_SI_EPG_TvProvider投影方針.md` を正とし、本書では列名・写像条件を再掲しない。
  - content / component / audio component / parental rating / series / event group / linkage / unknown descriptor のprovider-data schemaとcanonical encodeは `arib_si_engine_rs/DESIGN_JA.md` を正とする。
  - `event_group_descriptor` は r51 で構造化保存対象に含めるが、予約追従利用は r53 の到達点で有効化する。
  - 字幕 track 検出、`TvTrackInfo` 通知、`onSetCaptionEnabled()`、ARIB 字幕 PES の libaribcaption C API 経路、字幕表示までを r51 の対象に含める。BML / data broadcast 実行環境、双方向データ放送 UI、データ放送 UI は恒久的に対象外とする。
  - CAS plugin が本実装されている前提を置かない。
  - 仮トークン または診断専用 トークン を 本番経路のスクランブル解除成功 として扱わない。
  - トークン 用語は次のとおり固定する。`production token` は r52 以降に CAS plugin 本体が発行する復号用の不透明参照値である。`fake token` は fake CAS / test 用、`diagnostic token` は診断注入用、`placeholder token` は 仮実装 CAS 境界確認用であり、いずれも 本番経路 使用不可である。

- arib_si_engine_rs:
  - TIS が必要とする SI / EIT / サービス検出 / Programs 登録用の構造化解析を担当する。
  - r51 の `TvProvider.Programs` 収集は EIT p/f actual `0x4E` に固定する。EIT p/f other `0x4F`、EIT schedule actual `0x50..0x5F`、EIT schedule other `0x60..0x6F`、長期 schedule window、サービス横断更新は r53 対象とし、r51 の scan/setup で schedule を短期補完として収集しない。
  - r51 では content、component、音声コンポーネント、視聴年齢制限、series、イベントグループ、linkage、unknown descriptor を構造化変換対象に含める。具体schemaは `arib_si_engine_rs/DESIGN_JA.md`、TvProvider投影は `ARIB_SI_EPG_TvProvider投影方針.md` を正とする。
  - 常時 EPG 収集、長期 schedule 収集、予約録画・追従録画向けの高度利用は r53 対象とする。

- CAS plugin:
  - r51 では 仮実装 とする。
  - 対応 CAS system id を 本番経路 capability として広告しない。
  - plugin を 本番経路のスクランブル解除成功 可能なものとして返さない。
  - TIS の ECM 送受信境界、B25 用 EMM 送受信境界、Tuner HAL の `IDescrambler` 境界、不透明 トークン 受け渡し試行までを接続確認対象とする。B1 用 EMM 境界は設けない。
  - スクランブル解除成功は r51 の 対応宣言 外とする。

r51 で 表明してよい視聴範囲は、対象周波数帯における **非スクランブルサービス** である。スクランブル サービスについては、サービス検出、CA情報、ECM セクションフィルター起動境界、B25 用 EMM セクションフィルター起動境界、MediaCas / CAS bridge 接続境界、Tuner descrambler 接続境界、診断までを対象とし、視聴成功は表明しない。B1 用 EMM 境界は設けない。

#### releaseごとのライブ再生codec集合

ライブ再生として対応宣言するcodec集合は本節を規範正本とする。TISのcodec選択・MediaFormat・decoder起動・first-output・失敗時runtime契約は `tis/DESIGN_JA.md` を正とし、同文書へrelease別一覧を複製しない。

| release | video | audio |
|---|---|---|
| r51 | MPEG-2 Video、H.264 / MPEG-4 AVC | MPEG-2 AAC / AAC、MPEG audio |
| r52 | r51に H.265 / HEVC を追加 | r51と同じ |

codec signalingを意味解析・metadataとして認識できることと、当該releaseでライブ再生成功を対応宣言することは分離する。上表外のcodecを認識しても、同表を更新せず再生成功scopeへ追加しない。

選局については、r51 で対象周波数帯に対する explicit tune と サービス検出 を成立させる。r51 では対象周波数帯のうち、publishable ∧ viewable ∧ r51対応codec を満たす非スクランブルサービスを Live TV 視聴の 対応宣言対象とする。具体的には、ARIB `service_type=0x01` は現行対応video ESを持つaudio-video / video-only、`service_type=0x02` は現行対応audio ESを持つaudio-onlyを含み、最終のchannel登録・再生条件は `tis/DESIGN_JA.md` を正とする。スクランブル サービスの視聴成功は r52 対象とする。



### r51 視聴年齢制限 / CAS未完成EPG追加契約

r51 のTISは視聴年齢制限を正式対応宣言対象に含める。ARIB ratingからAndroid `TvContentRating` / TvProvider標準列への具体写像は `ARIB_SI_EPG_TvProvider投影方針.md`、ライブ視聴制限runtimeは `tis/DESIGN_JA.md` を正とし、本書では値写像を再掲しない。

コンテンツレーティングをTvProviderへ投影するかどうかと、Live sessionで視聴制限をenforceするruntime処理は別責務とする。標準列投影は `ARIB_SI_EPG_TvProvider投影方針.md`、現在番組/rating解決、Provider fallback、blocking・通知・AV制御は `tis/DESIGN_JA.md` を正とし、本書では具体API呼出しや状態遷移を再定義しない。

CAS未完成のscrambled serviceでも、TIS設計が登録可能と判定するchannel / Programs / EPG / content ratingの保存をrelease scope上は妨げない。ただし平文ライブ視聴成功の対応宣言には含めない。再生不可時の具体的なAndroid通知reasonは `tis/DESIGN_JA.md` を正とし、本書では再定義しない。

### r52 の到達点

r52 は、AOSP標準MediaCasService上でMaleicacid CAS pluginを本番経路へ接続し、B25 / B1 のスクランブル解除成功を対応宣言可能にするとともに、現行productが対象とするtransport profileで必要なcodec認識・再生判定を拡張するリリースである。

r52で必須とするproduct-level範囲は次のとおり。

- CAS plugin:
  - AOSP標準MediaCasServiceにloadされるMaleicacid B25/B1 vendor CasPluginを本番経路として成立させる。
  - B25はECM/EMM、B1はECM-onlyのcapabilityとし、方式固有backend・動的鍵状態・session/token失効をCAS側責務に閉じる。
  - raw Kw / KsをTIS/Binderへ公開せず、Tuner descramblerが解決できるopaque token参照へ接続する。
  - plugin ABI、lifecycle、backend、ECM/EMM、token/key stateの具体契約は `cas_plugin/DESIGN_JA.md` を唯一の正本とする。

- TIS:
  - 標準MediaCas/Tuner SDK経路でCAS pluginとTuner descramblerを接続し、B25/B1の本番tokenを使うscrambled serviceのlive playbackをrelease scopeへ含める。
  - product-levelのclear-memory non-passthrough invariantを維持し、secure media pathを必須とするserviceを成功対応へ含めない。
  - 対象releaseのcodec scopeをgeneric Tuner→MediaCodec playback pathへ接続する。codec metadata、MediaFormat、decoder、first-output、unsupported時のruntime契約は `tis/DESIGN_JA.md` を正とする。

- Tuner HAL:
  - r51で準備した `IDescrambler` 境界を本番CAS token/key stateへ接続し、packet-level descrambleとraw/record pathのscrambled pass-throughを成立させる。
  - AIDL戻り値、token validation、PID relation、packet処理、診断は `TUNER_HAL_DESIGN_JA.md` を唯一の正本とする。

r52でどのcodec・transport profile・serviceを対応宣言するかは本節のrelease scope、各module内でそれをどう認識・再生・失敗処理するかは各DESIGN正本を参照する。未取得の規格本文や改定概要だけを根拠に、release scopeまたはmodule contractを推測で拡張しない。

### CA system IDの正本

本製品が選択する放送CAS方式と数値の対応は次表を唯一の規範正本とする。

| 方式 | CA system ID |
|---|---|
| ARIB STD-B25 | `0x0005` |
| ARIB STD-B1 | `0x0001` |

CAS factoryのdescriptor・support query・createPlugin()、TISのCA選択・ECM/EMM配送、結合検証はこの対応を参照する。各言語の定数は本表の実装表現とし、別の値や独立した方式表を正本化しない。Tunerはtoken内部からCA system IDを抽出せず、packet復号のためにこの方式表を複製しない。AOSP ClearKeyの互換試験用IDはAOSPの定義に従い、上記放送CAS方式の代用にしない。

### r52のMULTI2固定値と動的鍵状態

複数moduleに跨るMULTI2 parameterの所有と共有方針は本節を正本とする。B25とB1は別CAS方式であり、固定parameterを共通値として扱わない。

| 情報 | 所有と使用 | runtime更新 |
|---|---|---|
| B25 `system_key` / `cbc_initial_value`（`init_cbc`） | `tuner_hal2`がB25 packet復号に使用するB25専用の製品固定parameter | CAS pluginから配送せず、session登録・ECM更新の対象にしない |
| B1 `system_key` / `cbc_initial_value`（`init_cbc`） | `tuner_hal2`がB1 packet復号に使用するB1専用の製品固定parameter。B25値を代用しない | CAS pluginから配送せず、session登録・ECM更新の対象にしない |
| `odd_ks` / `even_ks` | CAS側がECMから成立させ、MediaCas session/tokenに対応付ける動的鍵状態 | 同じtokenの参照先をatomicに更新・失効する |

TISは標準`MediaCas.Session.getSessionId()`と同一bytesのopaque tokenだけをTuner `IDescrambler.setKeyToken()`へ渡す。Tunerはtoken bytesを解析してCA方式を判定しない。vendor内部のtoken解決結果はcurrent Ksまたはそれを使用できる内部鍵資源への参照に加え、その状態を成立させたCAS方式の識別を持ち、Tunerはその方式識別でB25/B1それぞれの固定parameterを選択する。方式識別はvendor内部metadataであり、AOSP公開token形式やTISとのAPIを拡張しない。

B25の`system_key` 32 byteと`cbc_initial_value` 8 byteはB25方式の固定parameterとして扱い、ECM由来KsやYakisoba credentialと同じ秘密性をproduct invariantとして要求しない。正しい方式の値を選ぶこと、vendor image上で意図しない改変を許さないことと、値そのものの秘匿性は別の要件として扱う。Yakisoba credentialの秘密管理はCAS統合側の別契約とし、この固定parameterの扱いから派生させない。

B1の固定parameterはB1方式として独立に接続し、値または取得契約が成立していない状態でB25値へfallbackしてはならない。B1 tokenを解決しても対応するB1固定parameterを利用できない場合は復号可能な鍵状態として扱わず、利用不能として拒否する。

AOSP公開契約はopaque tokenによる`IDescrambler.setKeyToken()`連携であり、動的鍵状態と方式識別を共有する具体的なvendor内部方式は公開契約にしない。特定のtransport・process配置・新規service・公開IPC・公開wire形式を正本で必須化しない。raw Kw / KsをBinderまたはTISへ出さない責務境界を維持する。

session/tokenと動的鍵状態の一意な対応、atomic update/revoke、stale token・後着結果の排除の詳細は`cas_plugin/DESIGN_JA.md`を正とする。Tuner側のtoken参照・方式別固定parameter選択・packet復号契約は`TUNER_HAL_DESIGN_JA.md`、その実装ownerは`tuner_hal2/DESIGN_JA.md`を正とし、CAS側のKs更新とTuner側の参照結合を同じ責務にしない。

### r53 の到達点

r53 は、予約録画、常時 EPG 収集、EIT 追従、長期 schedule 利用を製品機能として 対応宣言 可能にするリリースである。

r53 で必須とする範囲は次のとおり。

- rec:
  - 予約録画、時刻指定録画、EIT p/f 追従、番組延長・変更・中止への追従、競合判定を実装する。
  - `TvInputService.RecordingSession`、DVR/file output、`RecordedPrograms` 登録、`notifyRecordingStopped()` / `notifyError()` を AOSP/TIF 契約どおりに実装する。

- epgcollection:
  - 常時 EPG 収集、長期 schedule 収集、サービス横断の EPG 更新を実装する。
  - 実装後は、選局対象のうち `EPGがあるサービス` だけにフィルタをかける。
  - TIS / arib_si_engine_rs / TvProvider の責務境界を保ち、Tuner HAL を EPG 意味モデル の SSOT にしない。

- arib_si_engine_rs:
  - r53 の EPG 収集・予約・追従に必要な EIT schedule / descriptor / event identity / 差分更新を提供する。
  - 字幕本文処理は TIS 側の libaribcaption 経路に閉じる。BML / data broadcast 実行環境は引き続き恒久対象外とする。
  - イベントグループ、linkage、EIT schedule other、長期 schedule window、予約追従用 event identity を扱い、r51 で保存した `eventGroups` / linkage を安全条件付きで予約追従へ接続する。

### 恒久的なスコープ外

- 4K放送、ISDB-S3、STD-B61、MMT、TLV は恒久的にスコープ外とする。
- STD-B79 の ISDB-T2 / ISDB-T1.5 および STD-B80 の ISDB-T3 は製品が採用しない高度地上方式であり、frontend capability、scan、ライブ視聴、EPG収集、codec capabilityを含め恒久的にスコープ外とする。
- BML / data broadcast 実行環境、双方向データ放送 UI、データ放送 UI は恒久的にスコープ外とする。
- VHF 1〜12ch、共同受信固有の特殊候補表、CATV の VHF 1〜12ch 相当候補は恒久的にスコープ外とする。
- CATV候補表は C13〜C63 のみを対象とし、VHF 1〜12ch を CATV 候補として追加してはならない。

この節は、製品全体または複数モジュールに影響する恒久的なスコープ外だけを正本として持つ。Tuner HAL の API、capability、VTS/profile、AIDL戻り値に閉じる恒久非対応は `TUNER_HAL_DESIGN_JA.md` を正とし、本書へ重複宣言しない。

# 設計方針

## 組み込まれるターゲット
使用するAndroidは、[get_android_qcow2.sh](https://github.com/kazuki0824/android_device_maleicacid_androidtv_tools/blob/main/get_android_qcow2.sh) でビルドされるものを前提とします。

## Tuner HALが考慮すべきドライバの仕様
Tuner HALは、下記の事実を踏まえて設計を行うこと。
- px4 legacy driverの`slot` ABIは、`slot < 12`を相対番号、`slot >= 12`をabsolute TSIDとして解釈する。HALはAOSPのtyped `RELATIVE_STREAM_NUMBER`と`STREAM_ID`を区別し、表現可能な値をlegacy `slot`へ直接渡す。数値域からselector kindを推測せず、driver固有値をTISへ公開しない
- 今回のターゲットデバイスドライバのうちearth_pt1において、 DVB frontend が複数 delivery system を持つ場合がありえない
- 今回のターゲットデバイスドライバに AOSP Tuner HAL の resource として扱うべき物理 demux は存在しない

## BS/CS選局方法
日本のISDB-Sでは、BSは同一周波数帯で複数TSを扱い、CS110は本製品の選局契約上、周波数だけで選局する。

TISが生成するBSの通常製品requestは、backendにかかわらずIF周波数とAOSPのtyped absolute `STREAM_ID`を使用する。TISはdriver種別、相対slot、HAL内部capabilityを候補生成へ持ち込まず、通常scan・channel保存・再選局では`RELATIVE_STREAM_NUMBER`を使用しない。

HAL backendごとのselector受理値域、legacy `slot`への写像、selector未指定の正規化、caller-visibleな拒否結果は `TUNER_HAL_DESIGN_JA.md` のISDB-S selector契約を唯一の正本とする。本書ではその値域・戻り値を再掲しない。

## 日本向け scan / 選局契約

選局対象、対象周波数帯、BS/CS110 selector 境界、CATV 候補範囲、VHF 1〜12ch の除外、CATV C13〜C63 限定、BS/CS110 の selector 方針に関する設計契約 SSOT は本書に置く。

製品用 channel scan の実行主体と物理候補表の実装データ保持者は TIS とする。TIS は本書で固定した設計契約に従い、地上UHF、CATV C13〜C63、BS物理RF、CS110 の候補表を実装データとして持つ。BSの実行時入力とfallback可否は、本書「製品 scan 候補の規範値」に従う。CATV中心周波数、表示番号、サービス検出結果から作るchannel keyはTIS側の実装データと受信PSI/SIを正とし、fallback TSID表を受信後service identityのSSOTにしない。

Tuner HAL は日本向け scan plan、サービス candidate table、channel display number、remote control key mapping、TvProvider 登録用 メタデータを自前生成しない。Tuner HAL common、frontend_dvb、generic scan layer には、日本向け scan table、サービス candidate table、BS/CS110 TSID table を置かない。

px4 backendはscan policyを持たず、AOSPのtyped selectorをpx4 legacy APIへ写像するadapterに限定する。表現可能な値域、legacy `slot`への具体写像、表現不能値のcaller-visibleな結果は `TUNER_HAL_DESIGN_JA.md` を正とする。固定TSID→slot表、TMCC由来の暗黙変換、数値域によるselector kind推測を導入しない。

他文書に scan 候補値を例示する場合は、必ず「説明用」「非SSOT」と明記し、本書の設計契約および TIS の実装データを参照する。`TUNER_HAL_DESIGN_JA.md`、`tis/DESIGN_JA.md`、README、future_work に独立した候補表SSOTを作ってはならない。

frontend_px4はscan policyを持たず、AOSPのIF周波数とtyped selectorをpx4_drvの`freq_no / slot / addfreq`へ変換するだけにする。相対番号とabsolute TSIDはselector kindで検証し、legacy ABIで表現可能な値だけを`slot`へ直接渡す。本書の選局契約とTISの製品用候補表を置き換える変換表を持ってはならない。

px4のISDB-S frontendを製品へ公開するには、対象driverのlegacy selector ABIと `TUNER_HAL_DESIGN_JA.md` が要求するtyped selector写像が検証済みでなければならない。TIS側を相対番号へ分岐させて補償せず、frontend_px4へTSID→relative slot変換表を導入してはならない。公開値域と拒否結果は同設計正本を参照する。

VTS / lab profile と backend adapter の内部変換は、製品 scan 候補表と サービス検出 SSOT ではない。これらを根拠に TIS 側の scan table、BS/CS110 TSID table、TvProvider channel key、display number を生成してはならない。

## BS/CS110 stream selector境界
BSの通常製品経路ではTISはtyped absolute `STREAM_ID`を生成し、`RELATIVE_STREAM_NUMBER`を通常候補またはchannelデータへ使用しない。CS110ではchannel identityとしてONID/TSID/service_idを保持してよいがHAL frontend selectorへ転用せず、周波数のみの選局requestを生成する。

HAL側のselector未指定表現、backend別値域・写像・拒否結果は `TUNER_HAL_DESIGN_JA.md` のISDB-S selector契約を唯一の正本とする。本書はTISが生成するselector方針だけを所有する。

## BS/CS110 selector固定テスト
次を単体テストで固定し、必要に応じて静的確認で補強すること。

- TISがBSの通常候補をbackendにかかわらずtyped absolute `STREAM_ID`で生成し、driver名、frontend ID数値規則、HAL内部backend種別または相対slotを参照しないこと。AOSP公開`FrontendInfo.statusCapabilities`は候補source選択に使用してよい。
- frontend_px4が固定TSID→slot表またはTMCC由来の暗黙変換を持たず、TISがdriver名、frontend ID数値規則、HAL内部backend種別またはruntime errorで候補sourceを分岐しないこと。
- TISがCS110 tune requestにstream selectorを付けないこと。
- HAL backendのselector受理・写像・拒否については `TUNER_HAL_DESIGN_JA.md` の最低テストを実行し、本書へ同じ値域・戻り値表を複製しないこと。

## px4_drv系のロック方法
px4_drv の AOSP frontend lock状態、status capability、callback遷移の公開契約は `TUNER_HAL_DESIGN_JA.md` の「px4_drv ロック 方針」を唯一の正本とする。

px4_drv / product-level invariant として、採用driverは `kazuki0824/px4_drv` `feat/android-ddk` commit `c2236cc761d9d02e38a728f8cad0519610cd891b` を基準とし、`PTX_SET_CHANNEL` の既存lock確認に加えて、read-only `PTX_GET_LOCK_STATUS` が current `ops->check_lock()` 結果を副作用なく返し、read-only `PTX_GET_TMCC_PARTIAL_RECEPTION` が current ISDB-T TMCC partial-reception 状態を副作用なく返すことを必須とする。未確定状態および I/O failure は正常な unlocked / `false` と区別する。AOSPへの具体的な `DEMOD_LOCK` / event / `partialReceptionFlag` 写像、世代柵、`LOCKED` / `LOST_LOCK` 遷移は `TUNER_HAL_DESIGN_JA.md` を正本とし、本書で重複定義しない。

## px4_drv系のBS TMCC TSID readback
px4_drv / product-level invariant として、採用driverは上記基準commitで read-only `PTX_GET_TMCC_TSID_LIST` を提供しなければならない。同UAPIはpointerを含まない固定長の `num + tsid[12]` を返し、ISDB-Sのcurrent TMCCから確定した非0 TSIDだけをdriverのTMCC slot順で公開する。TMCC未確定は `-EAGAIN`、ISDB-S以外は `-EOPNOTSUPP` とし、I/O failureを空listまたは正常値へ丸めない。

AOSP `STREAM_ID_LIST` / scan callbackへの具体的な公開写像、generation fence、readiness、失効条件は `TUNER_HAL_DESIGN_JA.md` を唯一の正本とし、本書では重複定義しない。TIS、VTS profile、host toolはdriver ioctlを直接呼ばず、product default `tuner_hal2` の公開AIDL契約を経由する。

## px4_drv系のchardev open方法
px4_drv の legacy chardev は同一 physical chardev endpoint の二重 open を許さないため、frontend_px4 の tune/control 経路とライブ TS read 経路は同一 endpoint を独立に `open()` してはならない。1つの backend owner が1つの device-open resource を所有し、control / read view を内部で分ける場合も別の device-open ownership を作らない。TS reader 準備失敗等を理由にした同一 endpoint の再 open fallback、別 path への open fallback、TS read 用の独立 open は禁止する。

この single-open / single-ownership 制約は px4 backend の ライブ視聴、EPG section 取得、AV filter、DVR/record path の前提である。owner resource から control / read view を導出する具体 API、helper 名、単体テストの内部形状は本書では固定しない。product default の実装規約は `tuner_hal2/CODE_CONVENTION.md` §18「px4 single-open backend 実装規約」を正本とする。

## TS packet 処理、section assembly、SI/EIT 意味解析 の責務境界

「TS解析」という曖昧語を単独で使わない。次の正式語に分ける。

- TS packet processing:
  188-byte TS packet、PID、adaptation フィールド、continuity counter、transport_scrambling_control、payload offset を扱う処理。担当は Tuner HAL とする。

- section assembly:
  TS packet payload から PSI/SI section payload を復元し、セクションフィルター へ配送する処理。担当は Tuner HAL とする。

- PES / AV / DVR delivery:
  PES、AV payload、record / DVR raw TS を HAL filter / DVR 契約に従って配送する処理。担当は Tuner HAL とする。ライブAVは製品アーキテクチャとして clear-memory non-passthrough `MediaEvent`だけを使用し、secure-memory、tunneled、AV passthrough は恒久的に採用しない。共有領域+`dataId`方式とイベント固有fd+`dataId`方式の両方を正式経路とする。共有handle未取得、使用権解放済み、共有領域空き不足の場合はイベント固有fd方式を使う。AV payloadを通常FMQ / EventFlagへ載せる経路、およびAV filterを他filterのsourceとする経路は実装しない。

- SI/EIT 意味解析:
  Tuner HAL から届いた section payload をもとに、PAT、PMT、CAT、SDT、NIT、BAT、EIT、descriptor を解釈し、サービス、event、EPG、CA情報 の 意味モデル を作る処理。担当は arib_si_engine_rs と TIS とする。

- TvProvider projection:
  意味モデル を `TvContract.Channels`、`TvContract.Programs`、`internal_provider_data` へ反映する処理。担当は TIS とする。

Tuner HAL を サービス検出、ARIB文字列、サービス メタデータ、CA情報、EPG 意味モデル の SSOT にしない。Tuner HAL は packet / filter / delivery の責務に限定し、意味解釈と TvProvider 反映は TIS 側に閉じる。

# ARIB文字/字幕のデコード
自前 ARIB文字 decoder(Rust実装)とlibaribcaption を改変せずを併用する。  
自前 ARIB文字 decoder は字幕以外の SI/EPG 文字列用(サービス名、プログラム名、番組詳細説明などなど)に限定し、字幕処理は TIS 側の字幕 path で libaribcaption のみを使う。libaribcaption は C API のみを使用し、独自 C/C++ 薄層 は書かず、Rust 側 safe ラッパー で所有権、cleanup、Result化、Rust モデル変換を行う。


# 実装方針

## CAS

CASのrelease到達点は本書、CAS plugin内部契約は `cas_plugin/DESIGN_JA.md`、TISのMediaCas/descrambler orchestrationは `tis/DESIGN_JA.md`、Tuner HALの`IDescrambler`公開契約とpacket descrambleは `TUNER_HAL_DESIGN_JA.md` を正とする。本節では各moduleの状態遷移、戻り値、session lifecycle、token validation、retry / cleanup、診断分類を再定義しない。

r51ではCAS plugin本実装によるスクランブル解除成功を対応宣言せず、r52の本番CASへ接続可能な標準MediaCas/Tuner境界を先行して成立させる。r52ではAOSP標準MediaCasService上のMaleicacid B25/B1 vendor CasPluginを本番経路へ接続し、正本設計が要求する実token・鍵状態・descrambler経路でスクランブル解除成功を確認する。

module間の責務境界は次に固定する。

| module / 境界 | product-level責務 |
|---|---|
| `arib_si_engine_rs` | 放送sectionからCA descriptor等の放送factを構造化する。CAS/TIS product policyは所有しない |
| TIS | Tuner SDK / MediaCas標準APIを使ってCA fact、ECM/EMM、MediaCas session、Tuner descramblerを接続し、service-levelの視聴状態へ反映する。raw Kw / Ks、MULTI2 algorithm、card protocolは所有しない |
| CAS plugin | B25/B1方式固有のMediaCas plugin処理、backend、ECM/EMM、session/tokenに対応する動的鍵状態の更新・失効を所有する。packet-level TS descrambleは所有しない |
| Tuner HAL | AOSP `IDescrambler`、opaque token参照、PID relation、packet-level descramble、scrambled pass-throughとTuner側診断を所有する。ECM/EMM意味解析やCAS entitlement policyは所有しない |

B25はr52でECM/EMM経路を扱い、B1は `cas_plugin/DESIGN_JA.md` が定めるECM-only capabilityに従う。どのsection filterを起動し、どのMediaCas methodへ配送し、session/token/PIDをどう更新・解放するかは `tis/DESIGN_JA.md` を正とする。B25/B1 pluginの受付・失効・backend挙動は `cas_plugin/DESIGN_JA.md`、Tuner側のtoken値域・戻り値・packet処理は `TUNER_HAL_DESIGN_JA.md` を正とする。

本番TISからTunerへ渡すtokenの用語とrelease上の使用可否は、本書「r51 / r52の到達点」で定める。tokenの具体形式、validation、失効時の状態遷移は各moduleのDESIGN正本へ委ね、本書では再掲しない。

## 放送規格準拠

設計はAOSP公開契約に準拠し、放送ロジックは対象製品scopeに適用されるARIB規格を根拠にする。具体的な規範条項、意味解析、公開APIへの写像は各所有DESIGN文書を正とし、本書ではmodule内部の規格解釈を重複定義しない。

## Tuner HAL側に置くB25デスクランブル処理

packet-level descrambleをTuner HAL `IDescrambler`へ置き、CAS pluginへTS packet処理を移さないことをproduct-level module責務として固定する。復号成功・失敗、scrambled pass-through、PID/token/packet処理の具体契約は `TUNER_HAL_DESIGN_JA.md` を唯一の正本とする。

## 流用対象
ここでの流用とは、ロジックを参考にして真似して再実装することを指す。  
ゴールを満たすには、製品ロジックをC/C++で実装してはならないことに注意する。例外は、`libaribcaption`本体のリンク利用、およびFMQ/EventFlag、dma-buf/libdmabufheapなどAOSP native APIへ接続するための極薄shimだけである。libaribcaptionはC APIのみを使用し、独自C/C++ 薄層を書かない。Rust側に所有権、cleanup、Result化、Rust モデル変換を担うsafe ラッパーを置く。

- Tuner HALとデバイスファイルの界面...recisdbなどを参考にする
- arib_si_engine_rs及びTuner HALのsoft_demux系...mirakc,tsukumijima/libaribb25から、ノイズに対する頑健性が高くなる有用な実装を積極的にロジック流用し、Rust実装を作り上げる
- TIS の ARIB 字幕 track 表示経路...PMT の字幕 component から `TvTrackInfo.TYPE_SUBTITLE` を作り、`onSetCaptionEnabled()` と libaribcaption C API 経路を接続する。字幕 PES、trackメタデータ、表示経路は TIS 側の責務とし、BML / data broadcast 実行環境、双方向 UI、データ放送 UI は恒久対象外とする。
- TunerHALのIDescramblerB25デスクランブル処理...tsukumijima/libaribb25からロジック流用し、pure Rust実装を作り上げる。ただし、上の実装方針を優先するため、TS処理部分などは流用しない。
- CAS plugin...libyakisoba-cross
- rec...EDCB


## コーディング規則の置き場所

プロジェクト全体に共通するコーディング規則は、tv 直下の `GLOBAL_CODE_CONVENTION.md` に置く。Rust 全般に適用する `panic` / `unwrap` / mutex汚染 / ワーカー / FFI / parser 境界の規則は、このファイルの Rust 節を正とする。

モジュール固有のコーディング規則は、各モジュール直下の `CODE_CONVENTION.md` に置く。no-`panic`、mutex汚染、一般worker / lock / callback、FFI / parser、テスト自己参照禁止など全moduleに共通する規則は`GLOBAL_CODE_CONVENTION.md`を正とする。product default Tuner HALのAIDL error mapping helper、typed entry、worker / callback / FMQ / source boundary等の現行実装規約は`tuner_hal2/CODE_CONVENTION.md`を正とする。

`DESIGN_JA.md` には設計判断を書き、コード記法・`panic` 禁止・ロック helper の使い方のような実装規約は `CODE_CONVENTION.md` に寄せる。状態遷移、戻り値、資源寿命、失敗時の閉鎖対象は `DESIGN_JA.md` を正とし、実装記法、禁止構文、helper 使用規則は `CODE_CONVENTION.md` を正とする。規則の重複記載で内容が割れた場合は、内容の分類に従って正とする文書へ集約し、他方を参照記載へ直す。

# リリース物のルール

## future_work の収録・削除規則

`future_work` は、設計判断・規範契約・調査結論が未確定で、後続の設計作業として残す必要がある事項だけを収録する。現行仕様、実装済み範囲、完了判定、変更履歴の正本として使用してはならない。

設計判断と必要な規範契約が、この文書または各モジュールの `DESIGN_JA.md`、必要に応じて `INTEGRATION.md` 等の所有文書へ欠落なく正本化され、`future_work` 本文を参照しなくても設計を一意に読める状態になった時点を、その `future_work` 項目の削除時点とする。削除前に、正本文書から当該 `future_work` 項目への規範参照を除去し、残す必要がある意味を所有文書へ移す。

実装未完了、build 未実施、atest / CTS / VTS 未実施、実機確認未実施だけを理由に、設計完了済みの項目を `future_work` へ残してはならない。これらの未完了状態は、責務に応じて実装、`CHANGELOG.md`、`INTEGRATION.md` の統合条件、または `タスク完了判定の実施方法.md` に基づく完了判定で管理する。

後続リリースで設計または再評価すること自体が未完了事項である場合は、その対象リリースの `future_work` に置いてよい。ただし、その配置だけを理由に現行製品の対応能力、当該リリースでの実装確約、または現行設計の変更として扱わない。

指示なく、future_workディレクトリにアイテムを追加しないこと。  
いかなる場合もこの文書に登場しないモジュールを追加しないこと。  
tvディレクトリには、このファイル、README.md、AGENTS.md、GLOBAL_CODE_CONVENTION.md、TUNER_HAL_DESIGN_JA.md、タスク完了判定の実施方法.md、ARIB_SI_EPG_TvProvider投影方針.md以外のMarkdownを置かない。  
README.md は人間向けの入口文書とし、リポジトリ内の主要文書、モジュール、Codex 起動位置への案内だけを記載する。設計判断、状態遷移、戻り値、資源寿命、失敗時処理、完了条件、統合手順、変更履歴を定義してはならない。  
AGENTS.md は作業エージェント向けの入口文書とし、作業開始位置、LineageOS target 初期化、必読文書、最低禁止事項だけを記載する。設計判断、状態遷移、戻り値、資源寿命、失敗時処理、完了条件、統合手順、変更履歴を定義してはならない。  
詳細な完了判定、文書責務、設計判断、統合手順は、それぞれ `タスク完了判定の実施方法.md`、この `開発規則.md`、`TUNER_HAL_DESIGN_JA.md` または各モジュールの `DESIGN_JA.md`、各モジュールの `INTEGRATION.md` を正とする。  

実行可能権限が必要な製品・開発ツール以外のファイルに、不要な実行可能権限を付与しないこと。  

`タスク完了判定の実施方法.md`に沿って、TODOが中途半端に止まっている状態ではないことを確認する。  
成果物の機械的チェック"だけ"を行うスクリプトファイルの格納を一切禁止する。  

各モジュールのフォルダが含んでも良いMarkdownは下記のファイル種別のみとする。それ以外のドキュメントを追加してはならない。  
丸括弧内の規定に沿わない記載が含まれてはならない  
- README_JA.md(このディレクトリにあるものを第三者が使い始めるために必要な情報「だけ」を記載する)
- CHANGELOG.md(必ず新しいものから降順に記載せよ。違反していたらリリース否認となるため並び替えよ。リリース時の報告とCHANGELOGの当該リリース項目は、「主な修正内容」ではなく、実際に変更したすべての修正内容を列挙する。未達を見落とさないため、helper追加・public到達経路・文書追従・テスト期待値更新・未実行検証を分けて記載する。)
- DESIGN_JA.md(このドキュメントに含まれていない設計方針、合意した設計判断だけを記載する)
  - 例外的に `tuner_hal2/DESIGN_JA.md` は、既存 `TUNER_HAL_DESIGN_JA.md` と同じ公開契約・状態遷移・戻り値・資源寿命、0-S-3Bの`WorkerRuntime` / `WorkerHandle` / `WorkerFailureClassifier`論理契約、および「scan / tune worker terminal mapping」「ワーカー終了契約」を再定義せず、`tuner_hal2` 固有の構造差分と対応境界だけを記載する。
  - `tuner_hal2/DESIGN_JA.md` に、README相当の使い始め説明、CODE_CONVENTION相当の実装記法・禁止構文、INTEGRATION相当のproduct統合手順、CHANGELOG相当の履歴、○×表相当の判定結果・未達理由を書いてはならない。
  - `tuner_hal2` の使い始め説明は `README_JA.md`、実装規約は `CODE_CONVENTION.md`、product統合手順は `INTEGRATION.md`、変更履歴は `CHANGELOG.md`、完了判定はアーカイブ外の○×表を正とし、同一内容を複数文書に重複配置しない。
- CODE_CONVENTION.md(そのモジュールに特化したコーディング規則だけを記載する)
- INTEGRATION.md(実機組込時のMakefileへの追加方法、APEXの作成方法、priv-app / permission / product feature / boot receiver / product integration 条件を第三者であるユーザーに説明するためのドキュメント。Tuner HAL、CAS plugin、TISに限って許可する)

### 恒久文書・変更履歴・互換残骸の収録規則

一時レビュー用Markdown、一時変更履歴、レビュー返信下書き、移送監査表などの作業用文書をリリースアーカイブに同梱してはならない。作業中の内容を恒久化する場合は、設計判断は各モジュールの`DESIGN_JA.md`、実装規約は`CODE_CONVENTION.md`、product統合手順は`INTEGRATION.md`、変更履歴は`CHANGELOG.md`へ統合し、元の一時文書を削除する。

恒久的な変更履歴の正本は各モジュールの`README_JA.md`が案内する当該モジュールの`CHANGELOG.md`だけとし、同一変更履歴を別名Markdown、補助履歴ファイル、ソースコメントへ重複保持しない。

本プロジェクトの未公開版相互の後方互換だけを目的とするalias、互換field、旧APIは、現行のAOSP公開契約または採用済みの外部互換契約が要求しない限り、非公開化して残さず削除する。

CHANGELOG.md以外の場所(ソースコードも含む)に、過去に下された設計判断以外の古い情報や編集履歴っぽい情報を残してはならない。リリース前に必ず削除が必要。  
CHANGELOG.mdとログメッセージ出力とコンパイラ診断用の説明文とCIワークフローの名前以外は、コード内のコメントも含め日本語のみを使うこと。ルー語は望ましくないがやむを得ない場合は慣用上問題ない場合においては許可する。  
Rustのunsafe境界コメントでは固定ラベル`SAFETY:`を使用し、後続の説明本文は日本語で記述する。この例外は固定ラベルに限り、英語の説明本文には適用しない。
機械生成ファイルにgeneratorが固定で出力する文言、およびupstreamへそのまま適用するpatchのcontext・原文は、再生成可能性またはpatch適用性を維持するため原文を保持してよい。この例外を自作source comment、doc commentへ拡張してはならない。
各モジュールにおいて、atestで実行可能な単体テスト群を必ず用意すること。  

テストによる本体ソース自己参照・文字列検査の禁止と、設定・文書整合確認での補助的な`include_str!()`利用条件は`GLOBAL_CODE_CONVENTION.md`のRust test規約を正とする。  

# r51の受け入れ条件
フォルダ構成はsoongルートに直接tar -xzfで適用可能な形を想定し、vendor/maleicacid/tv/配下に成果物が入っていること。  

全体的なゴール、モジュールごとのゴール、リリース物のルール、設計方針、実装方針をすべて満たしていること。

## TIS / arib_si_engine_rs 設計契約の正本

TIS の runtime 状態、TvProvider 利用、snapshot transaction、executor / ロック、setup、retry、Direct Boot、ライブセッション優先順位、TvProvider failure semantics は `tis/DESIGN_JA.md` を正とする。

`arib_si_engine_rs` の PSI/SI/EIT / descriptor 意味解析、parser 診断、provider-data schema、canonical encode、正規化、保存上限、安定キー抽出は `arib_si_engine_rs/DESIGN_JA.md` と `arib_si_engine_rs/schema/*.schema.json` を正とする。

ARIB 意味objectから TvProvider 標準列および `internal_provider_data` への投影判断は `ARIB_SI_EPG_TvProvider投影方針.md` を正とする。本書はリリース到達点、module間責務、product-level invariantだけを所有し、これら各正本が所有する規範値、状態遷移、失敗処理、schema、runtime APIを再定義しない。
