# 完了判定の再発防止策

## 目的

この文書は、過去に発生した「完了条件を満たしていないにもかかわらず、完了済みと誤判定した」問題の再発を防ぐための確認手順を定める。

再発防止の核心は、次の4点である。

1. **ロジック確認を主確認にする。**
2. **検索は漏れ検出の補助に限定する。**
3. **本体コード、config、profile、test、Markdown を同じ契約の一部として横断確認する。**
4. **完了報告には、処理経路と証拠ファイルを必ず添える。**

禁止語検索、旧クラス名検索、ファイル名確認だけで「完了」と判定してはならない。

単体テストであっても、対象ソースを `include_str!()` 等で文字列として読み、`contains()` / `find()` / `split()` / 正規表現で「該当コードがある」と確認するだけの自己参照テストを完了証跡として使ってはならない。この形式はテスト自身の文字列リテラルで自己充足し得るため、ロジック確認の代替にならない。完了証跡にできるのは、入力、状態、公開API、内部helper、診断、queue、コールバック、ワーカー停止などの実動作を検証するテストである。設定ファイル・VTS XML・sepolicy・rc・設計文書・別モジュールSSOTとの整合確認に限り `include_str!()` による補助確認を許可する。

---

## 1. 完了判定の主軸を「文言」ではなく「ロジック経路」に置く

完了条件は、特定の文字列や旧ファイル名が消えていることではなく、**入力から出力までの処理経路が契約どおり成立していること**で判定する。

### 悪い確認例

```text
ReflectionTunerDescramblerBridge が消えているので完了。
```

### 正しい確認例

```text
TIS の CAS 処理経路で、
MediaCas 呼び出し、ECM/EMM 処理、トークン 取得、
Tuner Descrambler.setKeyToken、addPid/removePid までが、
reflection なしの標準 API 経由で到達する。
```

文字列の消滅は補助情報であり、完了条件そのものではない。

---

## 2. 各 TODO を「契約 → ロジック経路 → 完了条件」に分解する

各 TODO は、必ず次の形式に分解してから修正・確認する。

```text
問題:
  何が未達か

契約:
  AOSP標準 / 開発規則 / TODO受け入れ条件のどれに違反しているか

ロジック経路:
  どの入力がどの処理を通り、どの出力・状態変更に至るべきか

完了条件:
  その経路が成立したと言える条件

編集範囲:
  本体コード
  config / profile
  test
  Markdown
  build integration
```

特に重要なのは、**ロジック経路を必ず書くこと**である。ロジック経路を書けない TODO は、完了判定も不安定になる。

---

## 3. ロジック確認を最初に行い、検索はその後に行う

確認順は次で固定する。

```text
1. 契約を読む
2. 実装ロジックを読む
3. データフローを追う
4. config / profile / test / Markdown と照合する
5. 反例検索をする
6. 禁止語検索をする
7. デグレ確認をする
```

検索は主確認ではない。検索は、ロジック確認後に見落としを検出するための補助確認である。

---

## 4. 1つの契約について、入力から出力までを必ず追跡する

例えば「CS110 は周波数帯のみ」という契約なら、次を一連の処理として読む。

```text
TIS ScanPlan
  ↓
Channel identity / TvProvider 保存値
  ↓
TIS onTune(channelUri)
  ↓
TunerController の frontend settings 生成
  ↓
Android Tuner API 呼び出し
  ↓
HAL frontend settings 解釈
  ↓
px4 / DVB backend 変換
  ↓
scan / tune / VTS設定 / test expectation
```

このうち1箇所でも旧仕様が残っていれば未達とする。

---

## 5. config / profile / test / Markdown は「別物」ではなくロジックの一部として扱う

本体コードだけを見て完了判定してはならない。次の要素はすべて同じ契約の一部として確認する。

```text
本体コード:
  実際の処理

config:
  VTS / lab が何を 対応宣言するか

profile:
  対象ハードで何を前提にするか

test:
  何を正しい動作として期待しているか

Markdown:
  何を設計判断として宣言しているか
```

設計文書と test が違えば未達である。コードと VTS設定 が違っても未達である。

---

## 6. 反例検索は、ロジック確認後の漏れ検出に使う

反例検索は必須だが、主確認ではない。

使い方は次である。

```text
ロジックを読んだあと、
「この契約に反する旧実装が他に残っていないか」
を探すために検索する。
```

### 例: CS110 周波数帯のみ契約の検索語

```text
cs110
CS110
isdbs_cs
streamId
stream_id
relative_stream
RELATIVE_STREAM_NUMBER
tsid
```

検索結果は必ず次に分類する。

```text
許容:
  BS 用の relative stream
  AOSP API で未指定を表す INVALID_STREAM_ID
  設計上許容された記述

不許容:
  CS110 candidate に relative stream を持たせる
  CS110 profile に stream_id を持たせる
  CS110 test が relative stream を期待する
  CS110 VTS設定 が streamId を 対応宣言する
```

不許容ヒットが1件でも残る場合は未達である。

---

## 7. 禁止語検索は最終の残骸確認に限定する

`TODO`、`FIXME`、旧クラス名、`not implemented` などの検索は有効だが、完了判定の中心にしてはならない。

```text
禁止語検索で分かること:
  残骸があるかもしれない

禁止語検索では分からないこと:
  設計が成立しているか
  API 呼び出しが正しいか
  実装経路が閉じているか
  config / test と整合しているか
```

禁止語ゼロでも、ロジック確認を通っていなければ静的OKとはしない。

---

## 8. 「代表箇所確認」を禁止し、全経路確認にする

「このクラスは直っているから OK」と判定してはならない。同じ契約に関わる経路を全て確認する。

### 悪い確認例

```text
Tuner descrambler bridge は直したので reflection は排除済み。
```

### 正しい確認例

```text
Tuner descrambler 呼び出し経路、
MediaCas 呼び出し経路、
CAS bridge、
ECM/EMM 処理、
トークン 受け渡し、
test double、
product 経路の全てで reflection がない。
```

代表箇所だけを見ると、別経路に残った未達を見落とす。

---

## 9. 「完了」と言う場合は、証拠をファイル単位で示す

完了報告は次の形式にする。

```text
項目:
  TIS-2 CAS/descrambler 標準 API 経路

完了条件:
  TIS の MediaCas 呼び出しと Tuner descrambler 呼び出しが標準 API 経由であり、
  reflection が製品経路に残っていない。

ロジック確認:
  - CasController が MediaCas を型付き API で生成する
  - processEcm/processEmm が型付き API 呼び出しである
  - TunerController が openDescrambler を型付き API で呼ぶ
  - setKeyToken/addPid/removePid が型付き API で呼ばれる
  - 仮トークン は成功トークン として扱わない

確認ファイル:
  - tis/.../CasController.kt
  - tis/.../TunerController.kt
  - tis/.../MaleicacidLiveSession.kt
  - tis/Android.bp

反例検索:
  - Class.forName
  - getMethod
  - invoke
  - Reflection
  - MediaCas

判定:
  静的OK / 未達あり
```

証拠を出せない項目は、完了扱いにしない。

---

## 10. 修正後は必ずデグレ確認を別フェーズで行う

修正直後は、直した箇所に意識が偏る。そのため、確認を次の3フェーズに分ける。

```text
フェーズ1:
  修正した箇所が完了条件を満たしたか

フェーズ2:
  同じ契約に関わる別経路に旧仕様が残っていないか

フェーズ3:
  修正によって過去に満たしていた条件を壊していないか
```

config / profile / test への旧仕様残存は、フェーズ2で検出する。

---

## 11. 「静的OK」「ビルドOK」「atest OK」「VTS OK」「実波OK」を分離する

次を混同してはならない。

```text
静的OK:
  ロジック、設定、テスト、文書を読んだ範囲で既知未達がない

ビルドOK:
  実際にビルドが通った

atest OK:
  単体テストが実行され成功した

VTS OK:
  VTS が実機で成功した

実波OK:
  ISDB-T / BS / CS110 の実波で scan / tune / 視聴 / DVR が成功した
```

ビルド前に言えるのは最大でも **静的OK** である。

「VTS/CDD合格想定」と言う場合も、**静的に VTS/CDD を阻害する既知問題がない**という意味に限定する。

---

## 12. AOSP標準とプロジェクト判断を分ける

完了条件の根拠は必ず分ける。

```text
AOSP標準で決まるもの:
  - TIS は TvInputService として実装する
  - TIS は Tuner SDK API 経由で Tuner HAL を使う
  - notifyVideoAvailable は Surface 表示後
  - TvProvider に channel/program を登録する

プロジェクト判断で決まるもの:
  - CS110 は周波数帯のみ
  - r51 は EIT p/f 中心
  - descriptor は完全変換
  - r51 は実復号成功を r52 に送る
```

AOSP標準で決まらないものを「標準だから1通り」とは言わない。

プロジェクト判断で固定したものは、`DESIGN_JA.md`、実装、config、test に反映されているか確認する。

---

## 13. スコープ外は「残してよい」ではなく「製品経路から外れている」ことで判定する

r52 / r53 送りのものは、単に「あとでやる」と書くだけでは不足である。

```text
スコープ外として成立する条件:
  - manifest に出ていない
  - VTS設定 で 対応宣言 していない
  - product package に含まれていない
  - 実行経路から呼ばれない
  - DESIGN_JA.md に r52/r53 送りと明記されている
```

スコープ外コードが製品経路に残っていれば未達である。

---

## 14. “検索でOK”という判定文を禁止する

次のような判定は禁止する。

```text
禁止:
  grep で出なかったのでOK
  TODO がないのでOK
  Reflection という文字列がないのでOK
  key-slot が消えたのでOK
```

代わりに、次のように書く。

```text
許可:
  ロジック経路を確認した結果、契約に反する経路は見つからなかった。
  その補助確認として反例検索を行い、不許容ヒットはなかった。
```

---


## 証跡ベースの完了判定ルール

この章は、完了判定時に作成する証跡の形式を固定する。
実装者の自己申告、文章だけの「確認した」、または「対応したつもり」によって OK としてはならない。
各受け入れ条件について、**最終アーカイブ対象ツリー**の現在コード・設定・テスト・生成物・設計文書を読み直し、実際に実行した確認コマンド、検索式、出力、分類、判定を証跡として残す。

この章の規定は、前章までのロジック確認・反例検索・config/profile/test/Markdown 横断確認を置き換えるものではない。前章までの確認結果を、機械的に検証可能な証跡として固定するための追加ゲートである。

### 1. 証跡表にはコマンド・検索式・出力を必須とする

完了判定時は、各受け入れ条件について次の表を作成する。

| 条件ID | 要求内容 | 確認対象ファイル | 確認コマンド/検索式 | 出力要約 | 未分類ヒット有無 | 判定 |
|---|---|---|---|---|---|---|

判定は OK / NG / 未確認 のいずれかとする。

次のいずれかに該当する証跡は無効とする。

- 「確認した」「問題なし」「目視確認済み」などの文章だけで、確認コマンドまたは検索式がない
- 確認対象ファイルが書かれていない
- 出力要約がなく、何を見て OK としたか追跡できない
- 未分類ヒット有無が空欄である
- 最終アーカイブ対象ツリー以外の確認結果である

無効な証跡が1つでもある場合、完了判定は NG とする。
NG、未確認、または無効な証跡がある場合、アーカイブを提示してはならない。

### 2. 旧実装・旧契約の残存検索は、検索語とヒット分類を残す

データ構造、key、ID体系、API境界、生成物、設定ファイル、profile、test テストデータを変更した場合、旧形式が残っていないことを リポジトリ 全体で検索する。

証跡には、少なくとも次を残す。

- 検索語または検索コマンド
- 検索対象範囲
- ヒット件数または代表出力
- 各ヒットの分類

各ヒットは次のいずれかに分類する。

- 修正済みの新実装
- 互換用途として意図的に残すもの
- test テストデータとして意図的に残すもの
- コメントまたは過去説明として残すもの
- 修正漏れ

未分類ヒット、説明不能なヒット、または修正漏れが1つでもある場合、完了判定は NG とする。

例:

- `service_id` 単独 key を廃止した場合:
  - `BTreeMap<u16, ...>`
  - `service_id -> pmt_pid`
  - `pmt_pids: BTreeMap<u16, u16>`
  - JNI / FFI / API で service_id 単独 mapping を返す関数

- CS110 の streamId 指定を廃止した場合:
  - `stream_id`
  - `RELATIVE_STREAM_NUMBER`
  - `relative_stream`
  - `streamIdType`
  - `relative_stream_candidates`

- reflection を廃止した場合:
  - `Class.forName`
  - `getMethod`
  - `invoke`
  - `java.lang.reflect`

### 3. 既知 NG の再確認を必須とする

過去に NG と判定された項目、ユーザーから指摘された項目、または前回の完了判定で漏れた項目は、完了判定時に必ず「既知 NG 再確認」セクションへ載せる。

各既知 NG について、次の表を作成する。

| 既知NG ID | 過去のNG内容 | 修正ファイル | 再確認コマンド/検索式 | 現在の結果 | 判定 |
|---|---|---|---|---|---|

過去 NG が証跡表に載っていない場合、完了判定は NG とする。
「別の修正で直ったはず」という推測だけで既知 NG を OK にしてはならない。

### 4. use-def / call graph 確認を必須とする

関数シグネチャ、構造体フィールド、key 型、JNI/FFI の引数・戻り値、XML/profile の項目を変更した場合、次を確認する。

- その値を書き込む箇所
- その値を読む箇所
- その値を API / JNI / FFI / Binder / XML / profile / generator 経由で外へ出す箇所
- その値を test / テストデータ / 診断 / Markdown が参照する箇所
- 古い型・古い意味のまま残っている箇所

単一ファイル内の確認だけで OK にしてはならない。
Rust / Kotlin / XML / Python generator / Markdown をまたぐ場合は、境界ごとに確認する。

証跡には、確認した呼び出し元、呼び出し先、参照箇所、検索式を残す。
call site を追っていない項目は NG とする。

### 5. コンパイル blocker の静的確認を必須とする

実ビルドを実施できない環境でも、最低限次を静的に確認する。

- 変更した関数内に未定義変数がないこと
- 変更した関数シグネチャの全 call site が更新済みであること
- 追加・削除・改名した構造体フィールドの全参照が整合していること
- enum / sealed class / match / when の分岐漏れがないこと
- JNI/FFI の native method 名、引数、戻り値が Kotlin/Rust/C++ 側で一致していること
- XML/profile/generator/test の参照名が一致していること

ビルドツールが使える場合は `cargo check`、`m`、`atest`、Soong build などを実行する。
ビルドツールが使えない場合は「未ビルド」と明記し、上記の静的確認結果を証跡として残す。

未定義識別子、型不整合、call site 不整合が疑われる場合は NG とする。

### 6. 新規シンボル・新規定数・新規 test expectation の定義確認を必須とする

新しい関数、定数、型、診断 name、test expectation、設定名、profile 項目を追加した場合、定義と参照を リポジトリ 全体で確認する。

特に test で参照しているシンボルについては、同名の定義が存在すること、および期待値が実装ロジックと一致していることを確認する。

証跡には、次を残す。

- 新規シンボルまたは期待値
- 定義箇所
- 参照箇所
- 実装ロジックとの対応
- 確認コマンド/検索式

未定義シンボル、未使用の新規定数、実装と一致しない test expectation がある場合、完了判定は NG とする。

### 7. テスト専用 実装と product/debug 実装を区別する

診断 API、counter、dump、registry、test helper、mock、テストデータについては、次を必ず確認する。

- `#[cfg(test)]` の内側だけに存在しないか
- test source set の中だけに存在しないか
- product/debug build から観測できる経路があるか
- test helper を product-ready 証跡として扱っていないか

`#[cfg(test)]` 内または test 専用 source set だけで確認できるものを、product/debug 診断の証跡に使ってはならない。
product/debug 経路で観測できない診断を受け入れ条件の根拠にした場合、完了判定は NG とする。

### 8. generator / config / profile / test の再生成可能性を必須確認する

config、profile、generator、generated XML、test テストデータ が関係する修正では、次を確認する。

- 手書き config だけが更新されていないこと
- profile が generator の入力として十分な情報を持つこと
- generator から リポジトリ 内 config を再生成できること
- 生成結果と リポジトリ 内 config の差分がないこと、または差分が意図的で説明可能であること
- test が generator と同じ契約を検証していること

証跡には、次のいずれかを残す。

- generator を実行し、生成結果と リポジトリ 内 config の差分がないこと
- 差分がある場合、その差分が意図的であることの説明
- generator が未対応であるため NG としたこと

ビルド環境や必要ツールがなく generator を実行できない場合は、未実施として明記する。
その場合でも、generator 入力、出力テンプレート、リポジトリ 内 config の静的照合結果を証跡に残す。

ただし、generator が存在するのに、再生成または静的照合の証跡がない場合、完了判定は NG とする。

### 9. レイヤ境界ごとの横断確認を必須とする

変更内容が複数レイヤをまたぐ場合、該当する境界をすべて確認する。

- HAL 実装
- HAL config / VTS設定
- profile / scan plan
- generator / tool
- test
- JNI / FFI
- Kotlin / TIS 呼び出し側
- TvProvider / 診断 API
- Markdown 設計文書
- 開発規則との整合
- AOSP 標準契約との整合

該当する境界が未確認の場合は NG とする。
「このレイヤは無関係」と判断した場合も、その理由を証跡に残す。

### 10. 反例ベースの判定を必須とする

OK 判定前に、受け入れ条件ごとに最低1つ、全体で最低3つの反例を作り、その反例で破綻しないことを確認する。

例:

- 同じ service_id が別 TS / 別 ONID に存在する
- BS と CS110 で選局 identity の意味が異なる
- px4 と earth_pt1 で tune モデル が異なる
- scrambled サービス だが CAS トークン が未接続である
- EIT version 更新で event が削除される
- generator の出力と手書き XML が食い違う
- descriptor が存在するが TvProvider に直接詰めてはいけない
- reflection を廃止したはずなのに呼び出し経路に残っている

反例で破綻する場合は NG とする。
反例確認の証跡には、反例内容、確認対象、確認結果を残す。

### 11. 完了証跡は最終アーカイブ対象ツリーから生成する

証跡は、最終的にアーカイブする作業ツリーから生成する。
修正途中のツリー、別ディレクトリ、古い展開先、またはアーカイブ作成前と異なる状態に対する証跡は無効とする。

証跡ファイルには、少なくとも次を記録する。

- 対象ディレクトリ
- 作成するアーカイブ名
- アーカイブ作成直前の主要ファイル一覧
- 実施した確認コマンド/検索式
- 実施できなかった確認と理由
- 未確認項目の有無

最終アーカイブ対象ツリー以外から生成した証跡で OK 判定してはならない。

### 12. Fail-閉鎖済み ルール

次のいずれかに該当する場合、完了判定は必ず NG とする。

- 実装を読んでいない
- call site を追っていない
- 旧実装・旧契約の残存検索をしていない
- 証跡が文章だけで、検索式や確認コマンドがない
- 検索ヒットが未分類である
- 過去 NG の再確認がない
- テスト専用 実装を product-ready 証跡として使っている
- 新規シンボルの定義確認がない
- generator / config / profile / test の生成差分または静的照合確認がない
- API / JNI / FFI / XML / Markdown の境界を確認していない
- AOSP 標準契約との対応を確認していない
- 最終アーカイブ対象ツリーとは別の状態を見ている
- 「たぶん」「想定」「ビルドすれば分かる」を根拠にしている
- 未確認項目が残っている

NG の場合はアーカイブを提示せず、修正へ戻る。

### 13. 設計契約と実装規約の集約確認

Tuner HAL の完了判定では、状態遷移、戻り値、資源寿命、閉鎖側失敗対象が `tuner_hal/DESIGN_JA.md` の「Tuner HAL 状態遷移表SSOT」に集約されていることを確認する。`tuner_hal/CODE_CONVENTION.md`、`GLOBAL_CODE_CONVENTION.md`、本書に同じ契約が別内容で再定義されている場合は NG とする。

確認時は少なくとも次を行う。

- `DESIGN_JA.md` の表7と表8に、対象操作と対象資源が存在することを確認する。
- `CODE_CONVENTION.md` は実装記法、禁止構文、helper 使用規則だけを定義し、状態遷移の正を再定義していないことを確認する。
- `GLOBAL_CODE_CONVENTION.md` は全体共通規約だけを定義し、Tuner HAL 固有の状態名や閉鎖対象を定義していないことを確認する。
- future_work 由来の未解決契約を現行設計表へ混入していないことを確認する。

### 14. 完了判定時は判定者モードに切り替える

修正後の完了判定では、自分が実装した内容を正当化してはならない。
次の順で確認する。

1. 受け入れ条件を再読する
2. 過去に検出済みの NG 項目を再読する
3. 最終アーカイブ対象ツリーを固定する
4. 変更差分ではなく、現在の最終コードを読む
5. 旧実装・旧契約が残っていないか検索し、ヒットを分類する
6. call graph / data flow を追う
7. generator / config / profile / test の整合を見る
8. API / JNI / FFI / XML / Markdown の境界を見る
9. テスト専用 実装を product/debug 証跡に使っていないか確認する
10. 新規シンボルと test expectation の定義・参照を確認する
11. 反例で壊れないか確認する
12. 証跡表を埋める
13. 未確認、未分類、証跡不備が1つでもあれば NG にする

## 最終ルール

今後、完了と判定するには、少なくとも次を満たす必要がある。

```text
必須:
  - 契約が明文化されている
  - ロジック経路を読んでいる
  - 本体コード、config、profile、test、Markdown を横断確認している
  - 反例検索を行っている
  - 禁止語検索を補助確認として行っている
  - デグレ確認を行っている
  - 未実施範囲を明記している

禁止:
  - 文字列が消えたことだけで完了扱いする
  - 代表箇所だけを見て完了扱いする
  - config / profile / test を確認せず完了扱いする
  - 静的OKをビルドOK、VTS OK、実波OKのように表現する
```

この手順を満たさない限り、完了とは言わない。
