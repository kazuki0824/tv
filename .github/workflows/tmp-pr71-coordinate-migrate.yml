name: tmp-pr71-coordinate-migrate

on:
  push:
    branches:
      - fix/tuner-hal2-vts-auto-region
    paths:
      - .github/workflows/tmp-pr71-coordinate-migrate.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/tuner-hal2-vts-auto-region
          fetch-depth: 0
      - run: python .github/tmp_pr71_finalize.py
