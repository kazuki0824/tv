name: Temporary CAS review3 patch

on:
  push:
    branches:
      - design/aosp-mediacas-b25-plugin
    paths:
      - .github/workflows/tmp-cas-review3.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: design/aosp-mediacas-b25-plugin
          fetch-depth: 0
      - name: Patch CAS plugin SSOT
        run: python3 .github/tmp-cas-review3.py
      - name: Commit
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -- cas_plugin/DESIGN_JA.md
          git diff --cached --check
          git commit -m "fix(cas): unify B25 B1 plugin contracts"
          git push origin HEAD:design/aosp-mediacas-b25-plugin
