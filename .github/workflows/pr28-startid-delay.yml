name: pr28-startid-delay
on:
  push:
    branches:
      - agent/fix-commonization-boundaries
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-commonization-boundaries
          fetch-depth: 0
      - name: Apply contracts
        run: python3 .tmp/pr28_startid_delay.py
      - name: Commit and clean helper
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tuner_hal/DESIGN_JA.md
          git rm -f .tmp/pr28_startid_delay.py .github/workflows/pr28-startid-delay.yml
          git commit -m "design: fix filter startId and delay contracts [skip ci]"
          git push origin HEAD:agent/fix-commonization-boundaries
