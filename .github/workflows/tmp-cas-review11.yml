name: Temporary CAS review11 patch

on:
  push:
    branches:
      - design/aosp-mediacas-b25-plugin
    paths:
      - .github/workflows/tmp-cas-review11.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: design/aosp-mediacas-b25-plugin
          fetch-depth: 0
      - run: python3 .github/tmp-cas-review11.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -- tis/tests/src/com/maleicacid/tvinput/tis/CasControllerStateTest.kt
          git diff --cached --check
          git commit -m "test(tis): align per-system descrambler ownership"
          git push origin HEAD:design/aosp-mediacas-b25-plugin
