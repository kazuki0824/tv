name: PR28 review fixes
"on":
  push:
    branches:
      - agent/fix-commonization-boundaries
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-commonization-boundaries
          fetch-depth: 0
      - name: Apply review patch
        shell: bash
        run: python3 .tmp/pr28_review_fixes_20260815b.py
      - name: Commit design fixes and remove temporary files
        shell: bash
        run: |
          git config user.name "maleicacid"
          git config user.email "4982384+kazuki0824@users.noreply.github.com"
          git rm .github/workflows/pr28-review-fixes-20260815b.yml .tmp/pr28_review_fixes_20260815b.py
          git add tuner_hal/DESIGN_JA.md 開発規則.md
          git commit -m "design: finish SSOT and lifetime cleanup"
          git push origin HEAD:agent/fix-commonization-boundaries
